/* Pakistan CRM — Profile Page Driver */
/* No JS-initialised elements on this page — purely static HTML. */
/* Seed: src/profile.html loads assets/js/dashboard.js (broken path — file does not exist). */
/* No charts, no DataTables, no canvas, no flatpickr inputs. No lib scripts included. */
