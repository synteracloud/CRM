# Frontend Architecture Documentation

Frontend architecture documentation. Populated during Frontend Authority Capture phase.

This directory will contain: component architecture, page inventory, CSS/JS stack decisions, and frontend implementation notes extracted from `frontend/src/` and the DESIGN-SPEC.
