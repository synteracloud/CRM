# Pakistan CRM — Document Catalogue

**Last updated:** 2026-05-31 — REBUILD-PLAN.md closed; COMMERCIALISATION-PLAN.md created (C0–C6 anchor). Prior: Phase 6 wiring extension COMPLETE — all 75 pages wired + browser-approved, entries updated across 12 docs.
**Scope:** All .md files in the project — 105 active + 0 planned + 3 archived. (Count last verified 2026-05-28. Phase 5B code files — .py, .js — not counted in .md scope.)
**Purpose:** Ground-truth index built from full reads of every file. Use this to find any document, understand what it contains, and know why it exists. Paths reflect current folder structure.
**Linkage audit:** 2026-05-17 — 11 issues resolved. All cross-references verified clean.
**Production readiness audit:** 2026-05-17 — 93 gaps identified and fixed across 26 files.

---

## How to use

| I want to... | Go to |
|---|---|
| Start a new build session | §A → CLAUDE.md, then FRAMEWORK.md, then DESIGN-SPEC.md |
| Know where the project stands right now | §A → SYSTEM-SNAPSHOT.md |
| Check what pages are built and what's next | §A → PROGRESS.md |
| Understand the roadmap and phase gates | §A → REBUILD-PLAN.md |
| See the full task checklist with statuses | §A → PENDING.md (root) |
| Check version history and test counts | §A → CHANGELOG.md |
| Understand contribution rules and commit format | §A → CONTRIBUTING.md |
| Build a custom frontend page | DESIGN-SPEC.md → FRAMEWORK.md §25–26 → relevant b9-p spec in §E |
| Wire a frontend page to backend APIs | §E b9-p spec → §B → FRONTEND-BACKEND-MAPPING.md |
| Review QC artefacts for a built page | §A → SCREEN-ARTEFACTS.md |
| Review backend QC pass | §B → BACKEND-QC.md |
| Check build constraints | §B → CONSTRAINTS.md |
| Check backend blocked items (P-016/P-017) | §B → backend/PENDING.md |
| Review Pakistan market context and pricing | §A → PRODUCT-SPEC.md |
| Check gap registers (code audit, product spec, market research) | §B → product-spec-gap-register.md · market-research-gap-register.md · PENDING.md §Phase 4 |
| Check public API routes and test counts | §B2 — Phase 3 public API layer |
| Check DB schema for a service | §C database docs |
| Understand gateway middleware | §D gateway docs |
| Find the entity fields and states for a domain | §F → domain-model.md or the relevant domain spec |
| Understand which service owns what | §F Core architecture → service-map.md |
| Check API standards before writing an endpoint | §F Core architecture → api-standards.md |
| Find all system events and their payloads | §F Core architecture → event-catalog.md |
| Understand auth/RBAC rules | §F Identity → identity-auth-rbac.md + security-model.md |
| Check Pakistan adapter architecture | §F Pakistan-specific → pakistan-adapter-architecture.md |
| Understand retry/DLQ/OCC rules | §F Infrastructure → execution-hardening.md + concurrency-control.md |
| Review why an architecture decision was made | §G ADRs |
| Find a Sprint 0 design doc | §H — cases, shared-inbox, compliance, localization, territories, pricing, etc. |
| Find a Phase 5B domain spec (marketing, partners, AI) | §M — marketing-campaigns.md, partners.md, ai-predictive-models.md |
| Find non-negotiables or permanently blocked items quick-reference | §I — Non-Negotiables and Permanently Blocked Items |

---

## §A — D:\CRM\ root (15 active + 2 archived)

Build authority files. Claude reads these first every session.

| File | Description | Purpose | Status |
|---|---|---|---|
| `CLAUDE.md` | Session enforcement rules: mandatory seed audit protocol (5-step check for aside/header/scripts/links/CSS before building any page), current build phase declaration, path translation rules (ai/→app/, forms/→app/), AI section vs standard CRM shell distinction with anti-defect rationale. Build checklist (4 rules): Rule 1 — crm-custom.css link mandatory on every app/*.html page (DataTables header centering fix lives here); Rule 2 — DataTable 3-place alignment (thead th class + JS column className + crm-custom.css with `!important` — Place 3 required for ALL tables, including data-driven ones, not just static HTML); Rule 3 — NexLink card fixed height — standalone col-12 identity strip cards must have `style="height:auto"` to prevent content overflowing the card bottom (root cause: `.card { height: calc(100% - var(--bs-gutter-x)) }` in styles.css collapses below content height in a solo col-12 row); Rule 4 — filter chips must use nav-pills-custom, never btn-group. | Auto-loaded by Claude every session; defines non-negotiable build behaviors; prevents wrong page structure, missing crm-shell.js, broken internal links, DataTable body-cell misalignment, identity strip card overflow, and broken filter chip styles across all 75 custom pages. | Active |
| `FRAMEWORK.md` | Complete technical build reference §0–§32 (3401 lines): §0 mandatory pre-build checklist (10-step ritual), HTML template (§2), script load order (§3), CRM_PAGE registry (§18), seed-to-page table (§17 — 96 entries), chart/DataTable configs (§24), design-phase build methodology (§25 — 9-step artefact sequence), QC protocol T1–T4 (§26), NexLink component catalogue (§30 Parts 1–16: KPI card variants 1A–1E, chart cards, table cards, kanban/task board, notification feed, button catalogue, badge rules, avatar rules, page header, modal rules, header injection, progress bars, layout grids, seed screen map, 15 fidelity gates); §31 Frontend Build Protocol (10 non-negotiable rules, directory structure, HTML shell §3.1–§3.5, navigation wiring, dummy data standard, API wiring pattern, RTL compliance checklist, review gate G-01–G-10, page lock / phase lock / deviation handling definitions); §32 Seed-to-Archetype Normalisation Protocol (Phase 0–7: mandatory seed audit, script dependency audit, nav/SVG rules 2A–2D, chart fidelity rules 3A–3G, KPI fidelity rules 4A–4D, CSS class chain preservation 5A–5C, data binding convention 6A–6B, visual verification gate with 30+ structural/chart/KPI/table/color checks, known-deviations table of 14 past mistakes). | Single reference for building every custom page; prevents archetype drift, JS mis-ordering, chart type substitution, and component inconsistency; §31 defines the page-lock gate (G-01–G-10); §32 is the deterministic normalisation formula that must be followed for every new page; must be read before any new page is started. | Active |
| `FRAMEWORK-GAPS.md` | Library phase gap register — GAP-001 to GAP-006 resolved; remaining open bugs (GAP-008/018/019) already noted inline in FRAMEWORK.md. | Historical trace of library phase decisions; superseded by inline annotations in FRAMEWORK.md; retained as audit record. | **Archived → D:\CRM\_archive\** |
| `deployment-pipelines.md` | B1-P05 CI/CD deployment pipeline spec: 3 pipelines (runtime packaging with SBOM, infra tooling packaging with terraform/kubectl/helm, environment deployment flow dev→staging→prod with progressive rollout and manual approval gates); QC gate script (scripts/deployment/qc_gate.sh) fix/re-fix dual pass requiring 10/10 before release; deployable end-to-end criteria checklist. | Historical spec for CI/CD deployment pipeline; superseded by runtime-deployment.md §F which covers the deployment model in full; retained as audit record of the original pipeline design. | **Archived → D:\CRM\_archive\** |
| `DOC-READ-LOG.md` | Durable line-by-line read checklist for all 102 project .md files: ✓ = fully read, ⬜ = pending; organised by §A–§H matching DOC-CATALOGUE.md sections; 4-batch audit complete (Batch 1 §A–§G roots/db/gateway/ADRs, Batch 2 §E+§F core, Batch 3 §F core domain + infrastructure first 10, Batch 4 §F infrastructure tail + UI + QC + §H Sprint 0); running total updated after each batch; 101 ✓ / 2 W / 103 total as of 2026-05-22 (post-merge). | Provides cross-session continuity for the full doc audit; proves every catalogue entry was verified against actual file content, not assumed correct; drives the batch correction protocol; must be updated whenever new docs are added and read. | Active |
| `DESIGN-SPEC.md` | Master screen inventory: §2 Design Constraints; §3 Screen Inventory (75 pages, A–M archetypes — updated 2026-05-30: header updated to T1-T4 ✓ all 75 pages; A-01 note updated to reflect fixes; build state updated from "audit pending" to "wiring sprint pending"); §4 Build Phase Plan; §5 Archetype Quick Reference; §6 File Naming Conventions; §7 Blocked Surfaces. | Gates the custom design phase; determines which pages exist, their archetype, UI pattern, and build sequence; frontend build cannot start without consulting this document. | Active |
| `PRODUCT-SPEC.md` | Product specification consolidated from 3 source files (CRM_EXECUTION_OS_SPEC_v1, Pakistan Wedge Addendum, Manus AI market report): §1 system architecture (16 sections: identity, design principles, L1/L2/L3 layers, 6 engines, 10 domain capabilities, execution model, WhatsApp-first, collections, hardening, 4 integration flows, extensibility, completeness criteria); §2 Pakistan behavioral layer (15 sections: zero-friction capture, trust/control layer, follow-up enforcement phases, cash flow reality, time-to-value, low-discipline environment handling, mobile-first, ≤2-step rule, localization in PKR/bilingual); §3 market intelligence (customer segments SME/mid-market/enterprise, 5 pain points, 3 current workflows, 5 competitor profiles, PKR pricing benchmarks, customer feedback, gap analysis, opportunity zones, 15-feature winning set, SWOT, alignment summary). | Defines the "why" behind product decisions; informs feature prioritization, UI behavioral defaults, enforcement rule design, and any new capability evaluation. | Active |
| `SESSION-HANDOFF.md` | Session handoff 2026-05-31 (4th pass): wiring extension COMPLETE — 75/75 pages live + browser-approved, 5 inline stub routes (billing/integrations/governance/reports/communications), 0 blocked; next session: Component 3 Final Hardening (locust load tests, 80% coverage gate, CI/CD containers, phase close). | Resume point for the next session — read this first if SYSTEM-SNAPSHOT.md context is insufficient. | Active |
| `SCREEN-ARTEFACTS.md` | QC artefact records for all 75 custom pages. Updated 2026-05-31: wiring extension complete — G-04/G-05/J-03 sign-offs updated to ✓ 2026-05-31; all 75 pages now T1–T4 ✓ + browser-approved. Prior: 2026-05-30 — Phase 6 Component 1 complete — T1–T4 audit; 9 pages fixed. | Visual QC truth record; per-page T1–T4 status now reflects actual file read audit not AI self-certification. | Active |
| `DOC-CATALOGUE.md` | This file. Master index of all .md documents with Name, Description, and Purpose columns for every active document. Sections §A–§I. | Single audit anchor for cross-session continuity; ensures every new doc is registered so it is visible to future code audits; the only correct place to look for what documents exist. | Active |
| `CATALOGUE-MERGE-PLAN.md` | 7-step protocol for merging DOC-CATALOGUE-OPS.md and DOC-CATALOGUE-TECH.md into DOC-CATALOGUE.md: Step 1 inventory all three files, Step 2 identify delta (content differences), Step 3 section-by-section merge checklist (§A–§H), Step 4 post-merge grep verification (10 key strings per sub-catalogue), Step 5 entry count verification, Step 6 delete sub-catalogues + update cross-references (only after Step 5 passes), Step 7 final audit + DOC-READ-LOG.md update. Appendices A/B/C populated as work progresses. | Ensures the catalogue consolidation is done without skipping any entry; grep verification and count check are the safety gates before any file is deleted; must be completed fully before sub-catalogues are removed. | Active — COMPLETE |
| `PROGRESS.md` | Frontend page progress tracker: 171 built pages (96 library + 75 custom). Updated 2026-05-31: header reflects wiring extension complete — 75/75 pages wired to live API; 0 externally blocked; all browser-approved. | At-a-glance build state tracker; used at session start to understand what is built and what is next; prevents re-building completed pages. | Active |
| `REBUILD-PLAN.md` | **CLOSED 2026-05-31.** Build phases 1–6 complete. All 75 pages built + wired + browser-approved. 42 gateway routes. ~527+ tests. Closed banner added; RESUME POINT redirects to COMMERCIALISATION-PLAN.md; Phase 6 Component 3 row marked "absorbed into COMMERCIALISATION-PLAN.md"; Phase summary updated. Historical record only — do not resume work from this file. | Build phase historical record. No longer the active anchor — COMMERCIALISATION-PLAN.md has taken over. | **Closed** |
| `COMMERCIALISATION-PLAN.md` | **Active anchor from 2026-05-31.** Phases C0–C6: C0 Environment Seal (C: leak proofing, tool cache verification, baseline measurement); C1 DB Wiring (replace 42 in-memory stores with PostgreSQL); C2 Automated Test Suite (C2a backend 80% coverage, C2b API contracts, C2c Playwright E2E all 75 pages with PAGE-BUILD-PROTOCOL.md defect loop, C2d Locust load tests, C2e OWASP ZAP + semgrep + pip-audit); C3 Code Hardening (JWT refresh, Redis rate-limit, helmet.js, CORS, OTP, multi-tenant signup); C4 Render.com deployment + GitHub Actions CI/CD; C5 post-deploy smoke; C6 commercial launch v1.0.0. Includes session resumption protocol, non-negotiable rules, tool inventory (all D:), phase dependency map. | Active anchor for all commercialisation work. Read first every session alongside SYSTEM-SNAPSHOT.md. Supersedes REBUILD-PLAN.md for all current and future work. | Active |
| `PENDING.md` | Rebuild task checklist: Phases 1–5B + M + Phase 6 Components 1–2 + wiring extension complete (175/176, 99%). Updated 2026-05-31: header updated — 75/75 wired; wiring sprint block extended with Phase 6 extension line [x] (75/75). Final Hardening (Component 3) is next open section. Blocked items P-016/P-017 unchanged. | Task-level execution tracker; prevents duplicate work; ensures all gaps are resolved before phase gates; blocked items (P-016 credentials, P-017 Urdu) are clearly flagged. | Active |
| `README.md` | Root GitHub landing page: architecture diagram (4-tier stack), quick start for Docker + local Python + frontend, doc index table, key constraints summary (C-001 RTL, C-007 DUMMY_MODE, C-009 stub modes, P-016/P-017 blocked). | Entry point for new contributors and external reviewers; provides orientation without requiring internal doc knowledge; references CONSTRAINTS.md and CONTRIBUTING.md for deeper context. | Active |
| `CHANGELOG.md` | Version history from 0.1.0 to 0.39.0. Latest: 0.39.0 (2026-05-31) — wiring extension: 5 inline gateway routes, 5 JS drivers rewritten, crm-api.js 5 new namespaces, 3 spec docs amended, 75/75 pages wired + browser-approved. Prior: 0.38.0 — 12-page extension; 0.37.0 — MR-004 + MR-005; 0.36.0 — Wave 3 (31 pages). | Audit trail of what changed and when; used to understand what was built in each session; enables rollback decisions by mapping version to capability. | Active |
| `CONTRIBUTING.md` | Branch naming conventions (7 types: feat/fix/docs/refactor/test/chore/phase), commit format (semantic commits with type+scope, examples included), PR process (one concern per PR, spec refs required, all 96 pages HTTP 200 confirmation), non-negotiable rules table (RTL from day 1, all API calls routed through crm-api.js with DUMMY_MODE flag, JAZZCASH_STUB_MODE=true, no country logic in core/, never commit .env, all 96 pages must stay HTTP 200), code standards (ruff + black + ESLint), local dev setup commands. | Enforces contribution consistency; prevents format drift in git history; ensures any external contributor follows the same build discipline. | Active |
| `SYSTEM-SNAPSHOT.md` | Point-in-time system snapshot (2026-05-31, 4th refresh): overall grade (9.95/10, Frontend 10/10), phase completion table (Phases 1–5B + Phase 6 Components 1–2 + wiring extension ✓; Component 3 Final Hardening ← CURRENT), 75/75 pages live + browser-approved, 42 gateway routes, ~527+ tests. | Single-page executive reference for project state; answers "where are we?" without reading every tracking file; should be updated at the start of each phase or after any major milestone. | Active |
| `MAPPING-TRACKER.md` | Frontend↔backend convergence plan: B0–B9c batch sequence with dependency graph, read inventory (22/22 backend routes done, 14/14 JS drivers done, 2/2 spec docs done — all ✅ complete), full gap register G-001–G-024 + NEW-1/NEW-2/NEW-3 (27 total, all closed), session log. Phase M fully complete 2026-05-27. | Historical execution anchor for Phase M — all batches done; gap register closed; reference for wiring sprint planning when DUMMY_MODE is lifted. | Active |
| `PAGE-BUILD-PROTOCOL.md` | Build protocol for custom CRM pages. Updated 2026-05-28: Non-Negotiable Archetype-Driven Build rule added at top — all 75 pages built archetype by archetype; first page in archetype = reference implementation; no new archetype starts until current archetype fully locked. Phase 0–5 sequence unchanged. Canonical archetype wiring status table updated from fresh 3-category mapping analysis. | Defines the mandatory build protocol; archetype-driven build rule is non-negotiable — prevents structural drift between pages of the same archetype. | Active |

---

## §B — backend/ root (7 active + 1 archived)

Backend project authority files.

| File | Description | Purpose | Status |
|---|---|---|---|
| `README.md` | System identity ("Execution-First CRM OS"), 3 core guarantees (never lose a lead / never miss a follow-up / always know cash position), 5 architecture principles, 6 behavioral principles (Pakistan market), L1/L2/L3 layer table, 6 engines table, 10 domain capabilities table, 4 critical integration flows, key documentation index, Python env setup (D:\Python 3.12.10, venv at D:\CRM\backend\.venv, fastapi/uvicorn/pydantic versions). | Backend onboarding entry point; tells any developer what the system is and how to run it locally; references CONSTRAINTS.md for non-negotiable rules. | Active |
| `BACKEND-QC.md` | Consolidated backend QC log covering 11 sections all 10/10 or RESOLVED: Foundation, Execution Hardening, Integration E2E, Enterprise Depth, System Hardening, UI Experience, Final Supervisor, Behaviour Gaps (15 items), Consistency Pass (4 items), src/ Gap Register (37 items), Build Progress (Groups 1–9). Latest validation snapshot (2026-04-01): 207 pytest passed, 96/96 library pages HTTP 200, P-016/P-017 intentionally blocked. Phase 5B adds 271 further tests (see §N) — tracked separately from this Phase 4 record. Consolidated from 11 source files (all deleted). | Single proof of backend quality; reviewer's reference for "is the backend done?"; gates Phase 4 from proceeding without verification; consolidation means no separate per-module QC files remain; required reading before Phase 4 work to understand what was already validated. | Active |
| `CONSTRAINTS.md` | 17 build constraints register with severity, layer, status, description, resolution, and pending-item links: C-001 RTL day-1 (CRITICAL/UI), C-007 chain-check audit endpoint stub masking failures (returns valid:true when activity service is down — must return 503), C-009 JazzCash/Easypaisa stub_mode=True default (never disable without sandbox credentials + full payment→webhook→reconciliation cycle — CRITICAL), C-010 Urdu strings must be reviewed by native speaker before any customer messages. Also covers in-memory store shims (C-004/C-006), feature flags not implemented (C-008), phone formatter limitations (C-011), PostgreSQL-only schemas (C-012), audit log NO UPDATE/NO DELETE (C-013), enterprise feature visibility tiers (C-014), src/ missing gateway routes (C-015). | Non-negotiable rules that cannot be violated without rework risk; must be read before starting any new build layer; CRITICAL flags mark constraints with architectural rework consequences. | Active |
| `PENDING.md` | Pending work register: P-001 to P-034 organized in 9 groups + 6 build layers (Layer 1 Backend/Service Layer → Layer 6 src/ Enterprise Layer), build sequence diagram showing cluster dependencies, per-item file locations and dependencies, completion checklist (27 items done, P-016/P-017 blocked, P-033/P-034 inconsistently marked in checklist vs detail sections). | Authoritative record of what is blocked (P-016 credentials, P-017 Urdu) and what is completed; prevents re-attempting blocked items; defines the dependency ordering that must be respected. | Active |
| `gap-register.md` | Docs vs code gap register: 20 gaps, all resolved. | Historical trace of doc-code alignment; all content resolved and superseded by BACKEND-QC.md; retained as audit record of what was fixed. | **Archived → D:\CRM\_archive\** |
| `market-research-gap-register.md` | Pakistan market research gaps from Manus AI report (MR-001 to MR-007): Facebook/Instagram lead capture, one-click WhatsApp invoice+payment, voice note transcription, daily WhatsApp manager summary, Excel import/export, geo-tagging, Kuickpay adapter. MR-004 (daily WhatsApp summary) and MR-005 (Excel import/export) buildable now; rest blocked. All Phase 6 scope. | Determines which market-research-derived features are buildable now vs blocked; gates Phase 6 feature selection; separates research findings from implementation sequencing to prevent premature work. | Active |
| `product-spec-gap-register.md` | PRODUCT-SPEC.md overlay audit against all 81 repo .md files (2026-05-18): 10 new gaps (PS-001–PS-010) + 7 MR gaps confirmed, 3 Phase-5 build blockers identified (PS-001 cases, PS-005 localization, PS-008 territories), 4 architecture gaps, 3 feature gaps. All PS items resolved as Sprint-0 tasks in PENDING.md Phase 4. All 9 new docs written 2026-05-19. | Historical record of the product-spec gap audit; all PS gaps now resolved; retained as an audit trail of what was missing and what was built to fill it; documents the rationale behind Sprint 0 doc choices. | Active |
| `FRONTEND-BACKEND-MAPPING.md` | Frontend↔backend mapping log. **⚠️ Phase M gap closure claims (Section 6) are UNVERIFIED.** Section 1 (route inventory) sourced from gateway files — trustworthy. Updated 2026-05-30: header updated to reflect Phase 5B completion — all former Cat 2 "no gateway domain" pages now have backend domains (Cases/Inbox/Territories/Campaigns/Partners/Workflows/AI all mounted); Section 7 Cat 2 status column updated. 30 gateway route files now mounted (was 23). | Output log for backend mapping analysis; Section 1 route inventory factual; DUMMY_MODE=false wiring verification is Phase 6 task. | Active |

---

## §B2 — Phase 3 Public API layer + Audit Fixes (6 modules)

Service public-facing HTTP modules and background workers. All JWT-gated; webhook endpoints use X-Api-Key. Updated during pre-Phase-4 audit: added RBAC gates, tenant isolation, overdue scanner, send-invoice endpoint, conversation detail.

| File | Description | Routes | Tests | Purpose |
|---|---|---|---|---|
| `services/conversation/http/public.py` | WhatsApp webhook ingest (API-key auth), conversation list with tenant isolation, conversation detail with full message thread. | `POST /api/v1/webhooks/whatsapp` · `GET /api/v1/conversations` · `GET /api/v1/conversations/{id}` | `tests/conversation/test_whatsapp_public.py` (16) | Write boundary for all inbound WhatsApp traffic; anti-lead-loss guarantee depends on this handler running correctly; any new conversation-layer endpoint must be added here. |
| `services/collections/http/public.py` | Invoice creation and retrieval, invoice send (schedules WhatsApp reminders with dates), JazzCash/Easypaisa payment callbacks with auto-reconciliation, tenant-scoped invoice filtering. | `POST /api/v1/invoices` · `GET /api/v1/invoices` · `GET /api/v1/invoices/{id}` · `POST /api/v1/invoices/{id}/send` · `POST /api/v1/payments/callback/{provider}` | `tests/coll/test_collections_public.py` (16) | Write boundary for all invoice and payment lifecycle operations; payment callbacks must route here for confidence scoring and reconciliation; P-016 stub mode guard lives in this module. |
| `services/activity/http/public.py` | Activity log creation and retrieval, immutable hash-chain integrity verification endpoint. | `POST/GET /api/v1/activities` · `GET /api/v1/activities/chain-integrity` | `tests/activity/test_activity_public.py` (10) | Enforces the immutable audit trail guarantee; chain-integrity endpoint is the compliance verification surface; every domain mutation that emits an ActivityEvent depends on this module being available. |
| `services/activation/http/public.py` | Onboarding activation start (seeds pipeline + contacts + deals), WhatsApp simulation, deal-move action, activation status query. | `POST /api/v1/activation/start` · `/whatsapp-sim` · `/move-deal` · `GET /api/v1/activation/status` | `tests/activation/test_activation_public.py` (10) | Drives the <10-minute Aha moment target; wired to the frontend onboarding wizard; sandbox-to-production WhatsApp transition logic lives in this module. |
| `services/core/execution/http/public.py` | Dead-letter queue listing, per-item retry (requeue→claim transition), per-item requeue (reset dead_lettered→pending). Admin role required (claims.role == admin check). | `GET /api/v1/admin/dead-letters` · `POST /{id}/retry` · `POST /{id}/requeue` | `tests/execution/test_dlq_public.py` (10) | Provides operator-facing DLQ management without code changes; admin role gate prevents non-operators from accessing the failure queue; required for production incident remediation. |
| `services/followup/overdue.py` | Background scanner: queries pending FollowupTask records past due_at and marks them overdue. Runs every 60s in production as asyncio background task started in app lifespan. | — (background worker utility) | `tests/followup/test_overdue_scanner.py` (4) | Enforcement backbone of the follow-up guarantee; ensures no task stays PENDING past its due_at without escalation; a stopped scanner silently breaks the core business promise. |

---

## §C — backend/db/ (5 files)

Database schema documentation.

| File | Description | Purpose | Status |
|---|---|---|---|
| `activity_task_db/README.md` | Scope overview for activities/tasks database (B2-P04): three entity scopes (activity — immutable timeline records linked to CRM entities; task — actionable work items with assignment + due-date scheduling; task_schedule — recurring/delayed schedule definitions for background execution); self-QC target with 3 validation goals. | Documents the activity/task DB scope; reference for migration authors and any developer querying the activity store; must be kept in sync with activities-tasks.md entity definitions. | Active |
| `activity_task_db/self-qc.md` | QC validation record for activities/tasks DB (B2-P04): 3 checks — task entity_type check constraint scoped to timeline-enabled entities (lead/contact/account/opportunity/case/message_thread) + tenant FK; no orphan activities (tenant_id + entity_type both constrained); scheduling shape integrity (immediate/delayed/recurring with cron/run_at mutual exclusion, misfire/concurrency policies). Fix loop added task_assignment_method_chk constraint. 10/10. | Proves the activities/tasks schema was validated before the layer was released; audit evidence for the build gate. | QC record |
| `transaction_db/README.md` | Schema overview for the transaction database: billing, subscriptions, payments (B1-P05, B2-P08, B7-P01); table definitions; migration setup; transaction handling patterns; cross-reference to transaction-policies.md. | Documents the billing/financial DB schema; reference for migration authors and the billing service implementation team; must stay aligned with payments-revenue.md entity definitions. | Active |
| `transaction_db/self-qc.md` | QC validation record in two sections: (B2-P08) payments/revenue DB — payment aggregate, status history, revenue ledger, 10/10; (B7-P01) transaction integrity — UoW functions, policy documentation, 10/10. | Proves financial schema was validated against spec; especially critical given financial integrity requirements (UoW functions, idempotency); audit evidence for both billing and transaction hardening gates. | QC record |
| `transaction_db/transaction-policies.md` | Transaction boundary policies for the billing service: which operations must be atomic, write rules per entity, rollback policies on failure, idempotency enforcement at the DB layer. | Defines which billing operations are UoWs and how failures must roll back; prevents partial financial state from being committed; supplements execution-hardening.md at the DB implementation level. | Active |

---

## §D — backend/gateway/ (2 files)

API gateway documentation.

| File | Description | Purpose | Status |
|---|---|---|---|
| `README.md` | API gateway structure (B1-P03): middleware list (request-id, request-validation, response-wrapper, rate-limit-hook) with processing order; complete route map — 42 route prefixes mounted under /api/v1 (37 pre-wiring-extension + 5 Phase-6-extension: billing, integrations, governance, reports, communications); CPQ quote/order APIs; standard response wrapper format; forecasting APIs (B2-P06); audit APIs. | Documents the Node.js gateway layer; tells developers which middleware runs on every request and what APIs live at the gateway vs services layer; reference for adding new middleware or routes. | Active |
| `self-qc.md` | Gateway QC record in two sections: (B2-P02) accounts/contacts standards conformance (REST endpoints, payload conventions, tenant isolation, Account-Contact relationship, B2-P07 domain alignment, 10/10); activities/tasks resource patterns and scheduling validation (10/10). Updated 2026-05-30: Phase 5B route coverage note added — 12 new route files follow same standards; formal api-standards.md QC pass for Phase 5B routes deferred to Phase 6 Final Hardening. | Proves the gateway layer was validated against API standards and domain specs; Phase 5B routes are documented as pending formal QC. | QC record |

---

## §E — backend/docs/ — Frontend Page Specs (15 files)

B9-series UI specs. Each spec defines scope, layout, components, read model bindings, and backend wiring for one page archetype. Read the relevant spec before building any custom page.

| File | Description | Purpose |
|---|---|---|
| `b9-p01-dashboard-kpi.md` | 13 dashboard panel specs covering all read model surfaces; five-zone layout (posture/primary_kpi/execution_queue/trend_diagnostic/risk_anomaly); 13 named panels each mapped to a read model (TenantEntitlementOverviewRM through PlatformReliabilityAuditRM); role-gated entry per panel; widget drilldown, empty-state, and period-selector interaction rules; Pakistan-specific owner dashboard section (curated subset of panels 2.3/2.5/2.7/2.8); error states for 401/403/404/429/503. **Updated 2026-05-30:** §5 API Routes added for A-03/A-11/A-13. **Updated 2026-05-31:** §5 A-08 Communications Engagement Dashboard API routes added — GET /communications/engagement (CREATE v1-communications.routes.js) + GET /campaigns (EXISTS); CommunicationEngagementRM response shape documented with all 8 fields + delivery_open_click_reply_rate channel breakdown. Self-QC 10/10. | Build spec for all 13 dashboard surfaces; §5 now covers A-03/A-08/A-11/A-13 wiring approach — aspirational /reporting/* paths replaced with actual gateway endpoints. |
| `b9-p02-list-queue.md` | 11 list/queue surfaces. Updated 2026-05-28: Lead.stage canonical enum added to §2.1; Follow-up Queue (§2.7) corrected — escalation_level vocabulary (none/reminder/warning/escalated/reassigned replacing Soft/Medium/Strict), field names (escalation_level not enforcement_level, attempts_count not attempt_count, state not status); Collections Queue (§2.8) Invoice.amount_due corrected. §4 DataTable 3-place alignment rules. Self-QC 10/10. | Build spec for all 11 list/queue pages; filter chip vocabularies now aligned to normalized backend enums; any new list page must follow the canonical field names and values defined here. |
| `b9-p03-sales-cockpit.md` | Multi-view sales workspace: Views A–E (deal list execution table, kanban stage board with drag-to-transition, deal detail pane, forecast context rail, activities/tasks panel); stage transition guardrails with allowed matrix; zero-duplication surface map (pipeline/detail/forecast/tasks each have distinct ownership); workspace model (CockpitMetricBinding/CockpitView/SalesCockpitModel) bound to OpportunityPipelineSnapshotRM; pipeline-first keyboard shortcuts (A/W/L/T). Self-QC 10/10. | Build spec for the most complex CRM surface; defines all view modes, stage transitions, forecast binding to OpportunityPipelineSnapshotRM (not ForecastTotals), and workspace model; high deviation risk on this page without the spec. |
| `b9-p04-support-console.md` | Ticket-first support workspace. Updated 2026-05-28: CaseStatus enum added (OPEN/ASSIGNED/IN_PROGRESS/WAITING_ON_CUSTOMER/RESOLVED/ESCALATED/CLOSED from cases-domain.md); sla_state derivation documented (derived from SLA timers, not stored field; healthy/at_risk/breached); cases-domain.md referenced as entity contract. Self-QC 10/10. | Build spec for the support console; CaseStatus state machine and sla_state derivation now authoritative from cases-domain.md. |
| `b9-p05-marketing-workspace.md` | Marketing workspace with 7-step guided campaign flow (draft→active→completed); 5 named views (campaign_workspace, segment_builder, funnel_attribution, journey_status, performance_drilldown) each with named read model field bindings; UI Config Model (NodeType/UiNode/UiFlowEdge/WorkflowUiPane/IntegratedUiModel) for the integrated marketing/admin/workflow navigable graph. Self-QC 10/10. | Build spec for the marketing workspace; view names must match exactly (campaign_workspace not campaign list); UI Config Model is a navigable graph for the integrated marketing/admin/workflow UI, not a standalone journey visualizer canvas. |
| `b9-p06-entity-detail.md` | **13 entity detail surfaces** (was 12). Updated 2026-05-28: §2.5 Case Detail state machine; §2.8 Subscription Detail state machine; §4 Identity Strip height:auto rule. **Updated 2026-05-30:** §2.13 Invoice Detail (C-08) added — header strip, field contract (invoice_number/account_name/total_amount/paid_amount/status/line_items/subscription_id), button state gates, payment history section, immutability rule, backend work needed (GET /api/v1/invoice-summaries/:id). Self-QC 10/10. | Build spec for all 13 entity detail pages; C-08 Invoice Detail now fully specified. |
| `b9-p07-workflow-visual-ui.md` | Node-canvas workflow editor: node type palette, edge connection rules, action inspector panel, trigger/condition/action configuration, workflow validation report, execution replay view. Self-QC 10/10. | Build spec for the workflow visual builder; defines how workflow DSL definitions are visually authored and validated against the workflow catalog. |
| `b9-p08-builder-extensions.md` | Three builder/canvas extensions: Custom Object Layout Builder (drag-and-drop field placement into 12-column grid, palette with 11 field types including Relation/Formula, right inspector for field properties); Rule/CPQ Logic Builder (rule-table editor — not a canvas — with condition/action/priority/simulation panel); CPQ Approval Lane Board (kanban 4-lane: Pending Review/Under Review/Approved/Rejected, claim-before-action, rejection requires reason). Self-QC 10/10. | Build spec for the three builder surfaces not covered by b9-p07/p05; Custom Object Layout Builder covers field layout (schema editing is in b9-p09 §2.8 — different page); CPQ Logic Builder is a table editor, not a visual canvas. |
| `b9-p08-mobile-responsiveness-system.md` | Mobile responsiveness rules: 8 device classes (XS Mobile 320–359px through Wide desktop ≥1440px); 4/8/12-column grids; P0–P3 priority taxonomy; mobile navigation (5-slot bottom nav); 6 critical workflows with exit criteria (no action hidden >2 layers, no required hover, no clipped CTA at 320px); fix loop protocol. Self-QC 10/10. | Defines mobile/responsive requirements for every page; P0 priority rule ensures core actions remain accessible on field-sales-class devices; exit criteria gate any page before it can be shipped. |
| `b9-p09-settings-admin.md` | Settings/admin surfaces. Updated 2026-05-28: 14 sections (2.1–2.14), all G-pages covered. **Updated 2026-05-30:** §4 API Routes for G-01/G-03/G-06/G-07/G-08/J-05. **Updated 2026-05-31:** §4 API Routes extended — G-04 (GET /billing/plans, GET /billing/subscription, POST upgrade/downgrade, GET /billing/invoices, GET /billing/entitlements; references pricing-plans.md §7; P-016 stub noted), G-05 (GET/PATCH /integrations, POST /integrations/:provider/test; connection shape with status/credentials_set/api_key_last4; G-05 route discrepancy DESIGN-SPEC vs b9-p09 §2.7 resolved — /app/settings/integrations is authoritative). SELF-QC updated: all 8 settings pages now covered. Self-QC 10/10. | Build spec for all G-series and J-05 pages; §4 API Routes is the primary build reference — all 8 settings pages now have endpoint contracts, field shapes, and status (CREATE vs exists). |
| `b9-p10-reporting-analytics.md` | 7 reporting surfaces. Updated 2026-05-28: completely restructured to match DESIGN-SPEC.md H-01–H-07. **Updated 2026-05-31:** §2.7 H-07 Custom Report Builder API Routes added — GET/POST /reports/definitions (save/load report defs), POST /reports/execute (time-series response); valid metric_key values enumerated (8 from kpi-data-pipelines.md + 4 read model fields); report definition shape + execute response shape documented; all marked CREATE in v1-reports.routes.js. Self-QC 10/10. | Build spec for H-01–H-07; §2.7 now has complete API contract for H-07 wiring. |
| `b9-p11-form-wizard.md` | 12 wizard/form surfaces. Updated 2026-05-28: I-01–I-04 and I-06 simple entity creation forms added (§2.1–§2.6) — New Lead Form (2-step, phone dedup, canonical stage enum), New Contact Form, New Opportunity Form, New Case Form, CPQ (§2.5 summary → §2.7 full spec), Campaign Builder (I-06, backend-incomplete). Original 6 enterprise wizards renumbered §2.7–§2.12 (CPQ, Lead Conversion, Contract, Subscription, Custom Object, Onboarding). ≤2 steps rule throughout. Self-QC 10/10. | Build spec for all DESIGN-SPEC.md I-series pages; previously only I-05 was defined — now all 6 I-pages covered. |
| `b9-p12-audit-compliance.md` | Audit/compliance surfaces. Updated 2026-05-28: all 5 J-pages covered. **Updated 2026-05-30:** §2.6 J-05 API routes table added — GET/PATCH /privacy/consent, GET/PATCH /privacy/consent/:contact_id, GET/POST /privacy/requests; links to full field contract in b9-p09 §4. Self-QC 10/10. | Build spec for J-01–J-05; §2.6 now has complete API route table for J-05. |
| `b9-p13-inbox-communication.md` | 3 inbox/communication surfaces. Updated 2026-05-28: shared-inbox.md entities integrated; L-02 route unified; L-03 added. **Updated 2026-05-30:** §4 API Routes added for L-01 and L-02 — all inbox endpoints listed (conversations list/get, claim, handoff, sendMessage, presence, queues), all exist in v1-inbox.routes.js, MessageThread entity shape documented. Self-QC 10/10. | Build spec for L-01–L-03; §4 API Routes is the primary build reference for L-01/L-02 wiring; all endpoints confirmed live. |
| `b9-p14-ai-copilot.md` | 2 AI surfaces: AI Copilot Insight Panel (embedded in entity detail/sales cockpit; score card 0–100 with top-3 drivers, next-action suggestion from next-action.js toCardData(), risk flags with one-tap remediation, dismiss/snooze); Conversational CRM (/app/chat, floating panel, input→classify→5 intent response cards, Urdu input → same classifier → locale response, session-scoped history 20 exchanges). All AI advisory-only; no auto-action. Self-QC 10/10. | Build spec for AI surfaces; advisory-only constraint is absolute — no AI suggestion may trigger an autonomous mutation; bilingual Urdu support for conversational input must work through the same intent classifier. |

---

## §F — backend/docs/ — Domain & Architecture Specs (46 files)

Authoritative backend domain documentation. These define entity fields, states, service boundaries, and system behaviour. Frontend and backend must not drift from these.

### Core architecture

| File | Description | Purpose |
|---|---|---|
| `architecture-overview.md` | L1/L2/L3 layer model with non-negotiable dependency rules (Core never imports L3 Adapters); 6 platform engines with guarantees and doc cross-references; 40 services in 3 tiers (~6 Core / ~16 Domain incl. AI & Predictive Models / ~12 Platform) — updated 2026-05-30; CQRS-lite data flow diagram; multi-tenancy model (tenant_id on every entity); 4 critical end-to-end flows (lead capture, follow-up enforcement, payment reconciliation, activation). | Master architectural reference; any new service must fit within this model; layer dependency rules are enforced by CI; used when designing any new capability to determine which tier it belongs to and what it can depend on. |
| `domain-model.md` | 79 entities. Updated 2026-05-30 (second pass): Phase 5B Supporting Entities section added — 12 entities: SupportQueue, SLAPolicy, CaseEscalation (5B-1), InboxQueue, AgentPresence, ConversationHandoff (5B-2), MessageTemplate, CampaignSend, CampaignConversion (5B-4), DealRegistration, PartnerActivityLog (5B-5), WorkflowStep (5B-6). Updated 2026-05-30 (first pass): AI / Predictive Models section added (Sprint 5B-7) — LeadScore, ChurnPrediction, CLVEstimate, CopilotSuggestion, ScoringModel, FeatureContribution. Updated 2026-05-28: Lead entity — gateway divergence notes added (owner_user_id→owner_id, status→stage, phone→contact_phone_e164, score→priority; gateway adds contact_name, title, estimated_value, currency, notes, metadata; see Section 1.1 of FRONTEND-BACKEND-MAPPING.md). FollowUp entity — gateway PK note added (gateway uses task_id as identifier, not followup_id; rule_type PascalCase note added). User entity — gateway returns `id` not `user_id` noted. | Authoritative entity field reference; gateway divergence notes (added 2026-05-28) document where the Node.js gateway exposes a different shape from the domain definition — frontend must consume gateway shape; domain definition remains authoritative for service-layer implementation. |
| `service-map.md` | Full service catalog with Layer/Boundary/Responsibility/Inputs/Outputs/Dependencies columns for all 29 internal + 4 external services (33 total); includes AI & Predictive Models Service added Sprint 5B-7; domain coverage matrix confirming no boundary overlap; 4 separation guardrails (e.g., Notification Orchestrator ≠ Communication Service, Analytics & Reporting ≠ Data Warehouse). Updated 2026-05-30: AI & Predictive Models Service row added; advisory AI capability row added to Domain Coverage Matrix. | Defines who owns what; prevents cross-service write coupling; used when adding a new service to verify it does not overlap an existing boundary; also used when debugging event flows to find the correct owning service. |
| `capability-matrix.md` | 19 CRM capabilities mapped to owning/participating services and dependency lists; partner management capabilities referenced in service-map.md, not independently itemised as rows. | Maps product features to implementation services; used when building a capability to find which services are involved and which is the write owner; prevents duplicate capability implementation. |
| `data-architecture.md` | DB-per-service ownership rules; hot tenant overflow strategy (20% capacity threshold → throttle → rebalance → dedicated tier); event storage (transactional outbox + durable event log); event schema versioning (90-day deprecation window, skip unknown versions on consumer side); caching layers (L1 in-process / L2 Redis / Edge CDN); cache invalidation triggers on permission changes; 4 implementation phases with completion criteria. | Defines data layer rules; DB-per-service rule prevents cross-service write coupling; hot tenant overflow defines when a tenant gets dedicated infrastructure; cache invalidation on permission changes is a security requirement. |
| `read-models.md` | 13+ named read models. Updated 2026-05-28: cross-reference table; 3 untyped RMs fully typed. **Updated 2026-05-30:** 3 read model catalog entries corrected — TenantEntitlementOverviewRM, CustomerMasterHealthRM, PlatformReliabilityAuditRM: aspirational `/reporting/*` API paths replaced with actual gateway endpoints + client-side computation approach. AdminDashboardReadModel consumer API updated. | Authoritative RM catalog; `/reporting` paths have been removed — builders must use the actual endpoints specified. |
| `api-standards.md` | Base path `/api/v{major}/{resource}`; resource naming rules (noun plural); HTTP method semantics; query params (page/page_size/sort/fields/filter[]); standard success envelope `{data, meta.request_id}`; error envelope with 9 canonical error codes (bad_request→400 through service_unavailable→503); major-version URL strategy; 180-day deprecation policy; OAuth 2.0 Bearer only; async job requirements (Idempotency-Key header, 202 Accepted); global idempotency integration. | Enforces consistent API shape across all services; prevents divergent error formats, pagination schemes, and auth methods; any new endpoint must conform before shipping; the error code table is the canonical reference for HTTP response shapes. |
| `event-catalog.md` | All system events across ~20 workflows in `{domain}.{entity}.{verb}.vN` naming format; 6 partner channel events (partner.created.v1 → partner.commission.approved.v1); event envelope fields (event_id, tenant_id, occurred_at, payload); EventHandler/EventPublisher/EventSubscriber protocol interfaces; global idempotency processing rules (producer retries reuse same event_id; consumer dedupes by tenant_id+event_name+event_id). | Single source of truth for all events; any new event must be added here before being emitted; consumers use this to find correct event names and payload schemas; idempotency rules define how event replay is handled across the platform. |

### Identity, security, tenancy

| File | Description | Purpose |
|---|---|---|
| `identity-auth-rbac.md` | User/Role/Permission/UserRole/RolePermission/SessionToken entities with full field definitions; 7-step JWT authentication flow; 7 IdP token validation rules; 10-step RBAC middleware algorithm with pseudocode; permission cache TTL 60s; deny-overrides-allow conflict resolution; session revocation on role change; Role-based UI configuration types (VisibilityMode, AdaptationState, NavigationMode, UiSectionRule, UiConfig, ResponsiveElement/ResponsiveLayout). | Authoritative auth and RBAC spec; every auth decision, permission check, and role-based UI rendering must conform to this; permission cache TTL (60s) determines how quickly role changes propagate; deny-overrides-allow is a non-negotiable security principle. |
| `org-multi-tenancy.md` | Organization + OrganizationMembership entities; 3 APIs (Create Org, Assign User, Read Members); hard isolation invariants (tenant match + org membership both required); mandatory WHERE tenant_id+org_id predicates on all queries; 7-step auth evaluation flow; cache/event/search boundaries with org_id; Admin Control Center data structures (PanelCategory, VisibilityState, AdminPanel, ResolvedPanel, InteractionPattern). | Defines the tenancy boundary enforcement rules; every query, cache entry, and search index must respect these boundaries; used to audit any code path that reads or writes data to verify tenant isolation is correct. |
| `security-model.md` | 4 security principles; 8 RBAC roles (Tenant Owner through Platform Security Ops); permissions matrix (11 permissions × 7 roles); 5 permission evaluation rules; tenant isolation rules table; break-glass workflow (Platform Security Ops only, 2 approvers, 4hr TTL, read-only); rate limiting thresholds (10k/min per-tenant / 500/min per-principal / 20/min auth endpoints); permission context = SHA-256(sorted permissions); session revocation via Redis `revoked:{tenant_id}:{jti}`. | Defines security boundaries and role capabilities; rate limits are SLA-enforced; break-glass TTL and dual-approver requirement are non-negotiable; used when implementing RBAC middleware, rate limiting, and break-glass workflow; permissions matrix tells you exactly what each role can do. |

### Pakistan-specific

| File | Description | Purpose |
|---|---|---|
| `pakistan-adapter-architecture.md` | 4 non-negotiable architecture principles; L1/L2/L3 layer structure; 6 adapter responsibilities; bilingual EN/UR with LocaleAdapter.get_string() protocol; Pakistan tone guidelines (polite/firm/urgent) for WhatsApp messages; PaymentAdapter interface (5 methods); MessagingAdapter interface (4 methods); ComplianceAdapter interface (2 methods); AdapterError taxonomy (6 error types); extensibility guide for new country/provider; CountryAdapterBundle plug-and-play registry; CI import boundary enforcement (ruff blocks core→pakistan imports). | Defines how all Pakistan-specific logic is isolated from core; prevents core services from importing JazzCash/Easypaisa/WhatsApp SDKs directly; ruff import check is the automated enforcement mechanism; new country expansion plugs into CountryAdapterBundle without touching core. |
| `whatsapp-execution-model.md` | WhatsApp as primary execution interface (not a side channel); inbound/outbound message flows; Conversation entity as first-class domain object (key: tenant_id+channel+normalized_phone); 7 conversation states; lead + follow-up state machines; provider abstraction layer; failure categories + exponential retry (30s/2m/10m/30m with jitter); Conversational CRM model; intent detection taxonomy with priority order (payment_query > follow_up_response > lead_inquiry > support_request); anti-lead-loss guarantee (every inbound creates Contact+Conversation regardless of sender); enterprise deduplication engine (RuleDefinition/MatchEvidence/DuplicateCandidate/ManualReviewTask/MergeWorkflow). | Defines the WhatsApp integration model; anti-lead-loss guarantee is a core business promise that must never be broken; intent taxonomy priority drives automated lead routing logic; dedup engine prevents duplicate lead creation from forwarded messages; retry model drives the provider failure handling implementation. |
| `adoption-ux.md` | 6 behavioral design principles for low-discipline environments; 4 feature visibility tiers (Tier 1 always visible → Tier 4 hidden by default); automation-first defaults for Pakistan SMB context; time-to-value contract (<10min Aha moment); adoption success criteria table; cross-references to activation-model.md, followup-enforcement-model.md, activity-control-model.md, offline-sync.md. | Defines how the product adapts to low-discipline usage patterns in Pakistan's SMB market; visibility tier model determines when features should be shown vs hidden; used when designing any new feature's default visibility and automation behavior. |
| `activation-model.md` | <10-minute value delivery design; zero-setup principle (auto-provisions pipeline + inbox + dashboard on first login); auto pipeline creation with 5 stages (New/Qualified/Proposal/Negotiation/Won); WhatsApp sandbox capture default; sandbox-to-production transition (15-min dual-active window, real inbound takes priority); 4-step guided onboarding; seed data pack (5 Pakistani contacts + 4 deals with realistic timestamps); optimistic status definitions table (3 states × what's pending); 7-state activation machine (NOT_STARTED through RETENTION_HOOK_TRIGGERED); failure handling (3s timeout + 5 retry max); Aha moment = WhatsApp capture + deal stage change in ≤10min (P75 target ≤8min). | Defines the new-user activation flow end-to-end; P75 time-to-Aha is a tracked business KPI; optimistic status table prevents blank screens during slow bootstrap; sandbox-to-production transition spec prevents lost messages during WhatsApp credential upgrade; drives the activation API and frontend onboarding wizard. |

### Core domain specs

| File | Description | Purpose |
|---|---|---|
| `followup-enforcement-model.md` | Core policy: no follow-up = system violation; 3-phase enforcement ramp (soft/medium/strict — tenant-level setting); escalation ladder (T+0 reminder / T+2h warning / T+24h escalation / T+48h reassignment); state machine (pending/overdue/completed); escalation_level canonical values: none/reminder/warning/escalated/reassigned. Updated 2026-05-28: task payload casing note added — escalation_level is lowercase (domain canonical); rule_type is snake_case (domain canonical) but gateway returns PascalCase (TimeBased etc.); FollowUp gateway PK is task_id not followup_id. | Defines the Follow-up Enforcement Engine; escalation_level and rule_type casing discrepancies between domain doc and gateway are now documented; frontend must use task_id as identifier. |
| `activity-control-model.md` | 7 control objectives; mandatory dual event emission (activity_event + audit_event on every write); canonical event schema with integrity.hash / prev_hash / chain_seq fields; ownership transfer state machine (requested→approved→executed or rejected/expired); 3 lock types (record/field/process); immutable audit guarantees (append-only + hash chain + Merkle root checkpoint + hourly verification); activity feed (latest 500); employee performance KPIs; shadow tracking (§8.1: WhatsApp business webhook captures all traffic even without CRM UI). | Defines the Activity Control Engine; dual event emission is non-negotiable — every activity write must produce both an activity and an audit event; hash chain is the audit integrity backbone; shadow tracking is the mechanism for capturing activity when users bypass the CRM UI. |
| `activities-tasks.md` | Activity entity (immutable, 10 fields including entity_type, entity_id, event_type); Task entity with assignment_method (explicit/entity_owner_fallback/least_loaded_candidate), scheduling defaults by priority, and schedule shape constraints (immediate/delayed/recurring); Ticket/Case entity with full field table, SLA states (healthy/at_risk/breached — at_risk when ≤20% window remains), EscalationRule, EscalationAuditRecord; task APIs. Self-QC 10/10. | Detailed spec for activities, tasks, and tickets; the SLA at_risk threshold (≤20% remaining window) drives the SLA monitoring timer logic; assignment method fallback chain (explicit→entity_owner→least_loaded) determines how tasks are auto-assigned when no explicit owner is set. |
| `opportunities-pipeline.md` | Opportunity entity with full fields; canonical stages (qualification→discovery→proposal→negotiation→closed_won/closed_lost) with complete transition matrix including valid regressions and re-open path (closed_lost→qualification); invariants (is_closed/is_won/forecast_category); transition pseudocode; 7 APIs including mark-won/mark-lost convenience endpoints; ForecastBucket/OpportunityPrediction/ForecastTotals/ForecastResult entities; follow-up queue API spec (5 endpoints, GET /api/v1/followups). Self-QC 10/10. | Defines the opportunity pipeline model; transition matrix determines which stage moves are valid (enforced server-side); forecast category invariants drive revenue reporting accuracy; closed_lost→qualification re-open path must be implemented correctly to support deal recovery scenarios. |
| `cpq-quotes-orders.md` | Quote + Order entities with full fields; 7 APIs; 5-step conversion workflow (quote draft → price → accept → convert → persist order totals); idempotency requirement on quote acceptance. Self-QC 10/10. | Defines the CPQ quote-to-order flow; quote acceptance idempotency is critical — duplicate acceptance requests must not create duplicate orders; used when implementing the CPQ module and commercial acceptance API. |
| `payments-revenue.md` | Payment entity with 9 status states (initiated/authorized/captured/settled/failed/canceled/partially_refunded/refunded/chargeback); PaymentStatusHistory (immutable, append-only); RevenueLedger (3 entry types: recognition/refund/chargeback_adjustment); status flow with terminal states; invalid transition 409 response; Subscription lifecycle (7 states: draft→trialing→active→past_due→paused→cancelled/expired); PlanChange entity with cancellation rules; RevenueRecognition (earned+deferred=billed invariant); Usage Billing flow; MeterRateCard with flat/tiered/volume semantics (tiered = graduated, volume = all-units-at-top-tier-rate). | Defines the complete payment and revenue recognition model; the earned+deferred=billed invariant is a non-negotiable accounting constraint; tiered vs volume billing semantics prevent mis-billing; PaymentStatusHistory immutability prevents financial record tampering; used by billing service implementation. |
| `collections-engine-model.md` | Full collections lifecycle; Invoice aggregate state machine (unpaid/partial/paid/overdue) — **⚠️ OPEN CONFLICT: gateway returns draft/open/paid/void/uncollectible; see conflict note added 2026-05-28**; Payment aggregate with manual/cash types, verification_status (not_required/pending_verification/verified/rejected), auto_verify tenant setting; ReminderEvent; ReconciliationCase; confidence scoring thresholds (≥85 auto-match / 40–84 manual review / <40 unmatched); tie-breaking by nearest due_date; WhatsApp reminder cadence (T-3/T-1/T+1/T+7/T+15) with tone tiers (polite/firm/urgent); customer opt-out via STOP or "لاگ آف"; payment proof handling (proof.viewed activity event, signed ephemeral URL 1hr TTL); 10 missing payment cases with fixes. **§N (added 2026-05-19, PS-007): Manual Payment Proof — PaymentProof entity (proof_id, invoice_id, attachment_url, note, payment_method, submitted_amount, submitted_by, verification_status: pending/verified/rejected, verified_by, verified_at, rejection_reason, reconciliation_confidence=1.0 on verified); file upload spec (JPEG/PNG/PDF/HEIC ≤10MB, malware scan, 7-day signed URL); WhatsApp image attachment download flow (30-min CDN expiry window); 5 API endpoints (submit/list/detail/verify/reject); RBAC (submit=any, verify/reject=manager+admin); 3 event types; PENDING.md M-15 enforcement gate reference.** | Defines the Collections Engine — a core business guarantee for cash position visibility; §N Manual Payment Proof fills the PS-007 gap for cash + bank transfer hybrid payments that have no automated callback; confidence scoring thresholds determine automated vs manual reconciliation; reminder cadence and tone tiers drive WhatsApp collection message generation; customer opt-out mechanism is a compliance requirement. |
| `owner-dashboard.md` | 4-domain visibility guarantee (Leads/Deals/Revenue/Collections); 4 required panels. Updated 2026-05-28: all 17 non-canonical read model names replaced with canonical names from read-models.md (LeadFunnelPerformanceRM, OpportunityPipelineSnapshotRM, SubscriptionRevenueRetentionRM, ActivityTaskOperationalRM, EmployeePerformanceRM). | Defines the owner dashboard data model; read model names now canonical — previously used non-existent names (LeadOwnershipReadModel, InvoiceAgingReadModel, etc.) that do not exist in read-models.md. |
| `data-governance-ownership.md` | Single-writer authority principle; ownership version tracking; no-uncontrolled-data hard guarantee enforced by 4 control gates (ingest/write/nightly sweep/release gate); remediation SLA (Critical 24h / High 72h); §2 retention and §3 quality rules defer to data-governance-layer.md as authoritative. | Defines the data ownership model; single-writer principle prevents conflicting writes from multiple services; control gates enforce data quality at system entry points; remediation SLAs create accountability for data quality violations. |
| `contract-lifecycle-management.md` | Contract + ContractTerm entities with full fields; 6-step lifecycle (draft→review→approved→active→renewal_pending→terminated); term insertion rule (blocked in active/terminated states); renewal alert function with configurable lead time; 12 APIs covering full CRUD + lifecycle transitions. Self-QC 10/10. | Defines the contract lifecycle; the state machine rules (term insertion blocked in active/terminated) are critical business logic preventing retroactive contract modification; renewal alert function drives proactive contract management to prevent unintended lapses. |
| `data-governance-layer.md` | 6 governance planes; governance roles (Tenant Owner/Tenant Admin/Data Steward/Compliance Auditor/Integration Service/Platform Security Ops); 6 governance objects; retention rules table (5 data classes with hot/archive/deletion mode and timings); legal hold workflow (4 steps, immutable scope after activation, supersedes deletion); DataQualityRuleSet JSON schema with lifecycle_gate; right-to-erasure workflow (GDPR/PDPA, 30-day SLA, immutable audit logs retain structure but PII redacted); change audit policy; 18 hooks (5 sync + 4 data-layer + 5 async + 4 admin/workflow). Self-score 10/10. | Authoritative data governance spec; legal hold supersedes deletion (non-negotiable invariant); right-to-erasure 30-day SLA is a compliance obligation; DataQualityRuleSet JSON schema drives the data quality enforcement service; 18 hooks define where governance is enforced across every system layer. |
| `partner-channel-management.md` | Partner/PartnerRelationship/PartnerAttribution/PartnerCommission entity definitions; attribution model guardrails (split attribution weights must sum to 1.0); 3 channel workflows (onboarding/deal-registration/commission-approval); APIs across 4 groups; DealRegistration entity with window_end_at conflict prevention. Self-QC 10/10. | Defines the partner channel model; split attribution invariant (weights must sum to 1.0) prevents double-counting revenue; commission lifecycle (pending_eligibility→pending_approval→approved→paid/reversed) determines when partner payments are approved; partner is a sidecar — direct sales ownership is never overwritten. |
| `custom-object-framework.md` | Object definition with object_key (immutable), module_scope, ownership_model, lifecycle_state (draft/active/deprecated/archived); 11 field types (text/long_text/number/decimal/boolean/date/datetime/json/enum/multi_enum/lookup); field lifecycle (soft-delete before data purge); 3-layer validation (schema/rule/cross-entity); 4 relationship types (1-M/M-1/M-M/polymorphic); hybrid storage model (metadata tables + record envelope + EAV values); FieldDefinition/ValidationRule/ValidationViolation/CustomObjectDefinition/ObjectRegistration data structures; SYSTEM_FIELD_KEYS reserved list ([id, created_at, updated_at, created_by, updated_by, tenant_id]). | Defines how tenants extend the CRM schema; SYSTEM_FIELD_KEYS are reserved and cannot be used by tenant-defined fields; field soft-delete-before-purge prevents accidental data loss; hybrid storage model balances query performance against schema flexibility; drives the custom object admin UI and storage layer. |

### Infrastructure & reliability

| File | Description | Purpose |
|---|---|---|
| `execution-hardening.md` | ACID transaction boundaries for 5 critical flows (quote acceptance, subscription+invoice, payment ingest, workflow transition, scheduler replay); canonical UoWs (create_subscription_with_invoice_uow, advance_payment_status_uow, record_payment_event_uow); error taxonomy (4 categories: retryable-transient/retryable-contention/non-retryable-business/non-retryable-system); retry policy (exponential backoff: 1s base, 2x multiplier, ±20% jitter, 60s max, 8 attempts); OCC escalation thresholds (>15% retry rate or >3× p95 latency); distributed lock policy; checkpoint semantics for long-running operations (15-min max lease age); DLQ operator action API contract (retry/requeue/cancel with audit trail); VersionedRecord/LockLease entity model; 10 verification gates. | Defines execution safety contracts for financial/payment flows; canonical UoWs must be used and never bypassed; retry bounds prevent thundering herd; DLQ operator API enables manual remediation; any release touching payment or subscription flows must pass all 10 gates. |
| `global-idempotency.md` | Global idempotency key tuple: (tenant_id, http_method, canonical_route, idempotency_key); fingerprint binding (SHA-256 of payload); response_body_ref storage (inline ≤4KB / sha256 object-storage reference with fetch-on-replay); TTL minimums (24h standard; 7 days MUST for financial mutations — quote acceptance, order creation, invoice posting, payment capture, subscription transitions); consumer inbox pattern (event_inbox table, UNIQUE(tenant_id, event_name, event_id)); 6 mandatory domain constraints (lead conversion, opportunity close, quote acceptance, order creation, subscription, payment); replay-safe execution rules; 5 required metrics (idempotency.request.hit/miss/conflict, idempotency.event.duplicate/replay_applied). | Prevents duplicate writes, duplicate payments, and duplicate order creation across all services; financial mutation 7-day retention is non-negotiable; consumer inbox pattern is the standard for all event consumers; 6 domain constraints are the minimum idempotency protections required. |
| `concurrency-control.md` | Hybrid OCC/pessimistic strategy; version_no/updated_at/updated_by data primitives; write contract (conditional UPDATE with version check); 4 conflict classes (write-write/state-transition/invariant/external-effect) with HTTP status codes; canonical conflict response payload; entity-level application map covering 6 domain groups: A) Deals (Opportunity/Quote/Order), B) Tickets (Case/CaseComment), C) Billing (Subscription/InvoiceSummary/PaymentEvent), D) Workflow State (WorkflowDefinition/WorkflowExecution/ApprovalRequest), E) Partner Channel (PartnerCommission paid-immutability/DealRegistration exclusivity window — Sprint 5B-5), F) AI Outputs (CopilotSuggestion monotonic flags — Sprint 5B-7); closed-opportunity override workflow; child versioning guidance. Updated 2026-05-30: Sections E and F added. | Prevents lost updates and phantom reads across all domain entities; conflict classes map to specific HTTP status codes; entity map gives developers the correct lock mode per entity; override workflow prevents closed deals from being silently edited without approval and audit trail. |
| `distributed-lock-strategy.md` | Redis-based lease locks with monotonic fencing tokens; lock key model (lock:{env}:{tenant_id}:{domain}:{operation}:{resource_id}); 4-method API (acquire/renew/release/force_expire); force_expire authorization (platform.ops.write + audit_reason + incident_ref + no active heartbeat check); acquisition rules (acquire-before-read-modify-write, bounded wait, jittered retry); expiry rules (30s default TTL, 120s max with heartbeat, renew at 1/3 TTL, 15-min max cumulative); deadlock avoidance (global lexicographic key ordering); 20 protected operations across scheduler/workflow execution/billing/reconciliation/shared integrity. | Prevents duplicate execution for critical cross-service operations; the 20 protected operations list is exhaustive — any new critical operation must be added here; Redis+DB token reconciliation semantics make DB the authoritative truth (Redis is the execution guard only); deadlock avoidance rules are essential for multi-lock scenarios. |
| `offline-sync.md` | Local-first architecture with persistent device queue; CommandRecord schema (idempotency_key, command_type, entity_type, entity_id, payload, status, retry_count); 10 supported command types with offline-safety rating and conflict risk; conflict resolution strategies by entity type (server-wins for lead stage; append-both for activities; field-level merge for contacts; reject+re-entry for payments); partial batch failure client guidance (no "sync complete" until all commands terminal); security (device keystore encryption, signed commands); schema backward compatibility (additive = safe; breaking = 422 + full cache refresh; max 2 major versions behind); low-bandwidth API response size budget (50KB list / 20KB entity / 100KB sync / 5KB WhatsApp message). | Defines offline behavior for field sales users on Pakistan's 3G/EDGE networks; conflict resolution rules prevent data loss from concurrent offline edits; schema compatibility rules prevent old devices from corrupting data after an API upgrade; response size budget is essential for Pakistan's bandwidth constraints. |
| `scheduler-jobs.md` | 8 canonical job states (queued/leased/running/succeeded/retry_scheduled/failed_terminal/dead_lettered/canceled); Job record JSON shape with idempotency_key, priority, lease_timeout_seconds, retry_policy; lease reclaim atomicity (compare-and-swap UPDATE); deduplication guarantees (dedupe_window_seconds, returns existing job_id with deduplicated=true); Schedule record JSON shape with concurrency_policy (allow/forbid/replace) and misfire_policy (skip/fire_once/catch_up); catch-up rate limiting (2hr window max, FIFO, max 10 concurrent); 9 scheduler events published via outbox; 10-item release checklist. | Defines background job execution model; catch-up rate limiting prevents job cascades after prolonged outages; lease atomicity ensures no job runs twice; outbox transactionality guarantees events are not lost; any new recurring or delayed job type must conform to this model. |
| `feature-flags-config.md` | feature_flag entity (key, description, status, default_variant, rules[], owner_team, expires_at); flag_rule schema (rule_type: tenant_match/role_match/percentage_rollout/env_match; priority; variant); config_entry entity (namespace, key, value_type, value, scope, version); SHA-256 percentage rollout hash algorithm (deterministic bucket assignment); flag evaluation algorithm (6 steps); flag management/evaluation/config management REST APIs; change approval process (4hr timeout, 2-person, emergency override with 24hr post-review); expired flag evaluation behavior (auto-archive after 7 days); SLO (P95 ≤20ms, P99 ≤50ms). | Defines how features are enabled/disabled at runtime without deploys; change approval process prevents unilateral production flag changes; expired flag TTL policy prevents zombie flags from persisting indefinitely; SLO defines observability requirements for flag evaluation performance. |
| `integration-contracts.md` | All approved external integrations: Stripe/SendGrid/Twilio external APIs (endpoints, auth, retry); inbound Stripe/SendGrid/Twilio webhook contracts (verification, dedup, failure handling); 3 data contracts (CustomerSync v1, PaymentStatus v1, MessageDelivery v1); authentication method table for all 8 surfaces including JazzCash/Easypaisa callback verification (HMAC-SHA256, pp_SecureHash); Meta WhatsApp/Twilio WhatsApp/360dialog/Gupshup API+webhook contracts; 24hr dedup windows per WhatsApp provider; 360dialog/Gupshup key rotation procedures; GET /api/v1/forecasts spec (stage probability table); Plugin framework entities (PluginManifest/HookExecutionContext/PluginRecord); WebhookSubscription/WebhookDelivery entities; CI provider allowlist enforcement. | Defines all approved external integrations; CI checks enforce the provider allowlist so unknown providers cannot be added at runtime; any new integration must be added here before implementation; adapter interface compliance is enforced by architecture test; the provider allowlist is the single gating mechanism for third-party additions. |
| `observability-audit.md` | Structured JSON log event contract with base envelope (ts/level/service/env/event_type/trace_id/tenant_id/actor/target); 5 severity levels with retention policy (30d hot/180d archive/365d security/7yr audit); redaction rules (never log tokens/secrets/PII in request bodies); 7 mandatory event categories; W3C Trace Context propagation; 6 required spans for mutating APIs; sampling strategy (100% errors/security, 25% mutations, 1–5% reads, tenant override up to 60min); 7 core metrics counters/histograms; 4 alert hooks (authz deny spike, cross-tenant attempt, audit pipeline failures, append failures); immutable audit event schema with hash chain; 5 audit APIs (query/detail/export/export-status/integrity-verify); critical action coverage matrix (10 domains) with CI enforcement (fail if route has mutation scope but no audit mapping). | Defines observability and audit standards; CI enforcement prevents mutation endpoints from being shipped without audit mappings; 7-year audit trail retention is a compliance requirement; cross-tenant access attempt alerting is a security SLA requirement; the 10-domain critical action matrix is the completeness gate. |
| `kpi-data-pipelines.md` | 8 KPI definitions with explicit formulas, event field references, and derivation rules: Lead Conversion Rate, Opportunity Win Rate, Pipeline Value (state-derived from 3 events), Quote Acceptance Rate, Booked Revenue, Cash Collected, Invoice Collection Rate, Subscription Churn Rate; 4-layer aggregation pipeline (Bronze ingestion / Silver normalization / Silver+ state-build / Gold aggregation); canonical table schemas (analytics_raw_events, typed silver tables, gold_kpi_timeseries with PK); incremental update pattern; streaming consumer groups (kpi_leads/kpi_opportunity/kpi_revenue/kpi_billing) with per-partition tenant-id ordering; late-event correction (35-day reprocessing window); SQL-like aggregation examples. | Defines what KPIs are computed and how; formulas reference only catalog events, preventing fabricated metrics; event trigger map tells analytics consumers exactly which events to consume per KPI; gold table PK ensures idempotent upserts; late-event window defines how far back corrections are accepted. |
| `runtime-deployment.md` | 4-environment matrix (local/dev/staging/prod) with SLA targets and change policies; 36 required environment variables grouped by category; config precedence order (secret manager > orchestrator env > config file > defaults); 90-day secret rotation with 15-min dual-key overlap window; OCI container standards (SBOM, provenance attestation, non-root user, read-only FS, 3 permitted writable volumes); K8s baseline (2 replicas min, HPA, PodDisruptionBudget, NetworkPolicy default-deny, resource sizing table); canary rollout (5%→25%→50%→100%); rollback thresholds (>2% error rate or +200ms p95 sustained 3min); 5-layer infra blueprint (Foundation/Platform/Traffic/Application/Operations); RPO ≤15min / RTO ≤60min; 10-item QC checklist. | Defines how the system is packaged, deployed, and rolled back; rollback thresholds are monitored automatically during canary; permitted writable volumes list prevents arbitrary filesystem writes; DR targets set SLA expectations for recovery; 10-item checklist gates every production release. |
| `workflow-catalog.md` | 19 canonical business workflows each with: trigger events, services involved in order, ordered execution steps, and outcome statement; includes: tenant provisioning, identity lifecycle, lead intake+conversion, contact/account management, opportunity pipeline+close, quote+approval+acceptance, subscription+invoicing+payments, case management+SLA, communication engagement, notification dispatch, knowledge publishing, workflow orchestration, search indexing, feature rollout, governance+audit, platform reliability, scheduler job lifecycle, scheduler schedule lifecycle, partner channel management; lead conversion atomicity contract (saga: Account→Contact→Opportunity order, compensation on failure with revert to qualifying state). | Defines the business process backbone; every service knows its role in each workflow from this catalog; lead conversion saga contract prevents partial conversions from leaving orphaned entities; workflow-dsl.md definitions must align to these ordered steps. |
| `workflow-dsl.md` | Machine-readable DSL for workflow definitions: pseudo-syntax form (workflow_key, version, metadata, triggers, conditions, sequencing, actions); JSON form with full example (lead_intake_assignment_conversion workflow); alignment rules (workflow_key→catalog snake_case, triggers.events→event catalog, steps→catalog step semantics); 7 validation rules; action types (emit_event/call_service/notify/mutate_state/wait); minimal template. | Enables workflow automation to be configured as data, not code; validation rules prevent workflows from referencing non-catalog events; workflow_key alignment with catalog prevents drift between DSL definitions and business process specification; JSON form drives the Workflow Automation Service execution engine. |

### UI specs (backend-side)

| File | Description | Purpose |
|---|---|---|
| `ui-foundations.md` | Design system foundations (B9-P01): typography scale (11 tokens, Inter font family, 14px minimum for operational screens); spacing system (4px base, 11 tokens); radius/border/elevation tokens; 17 semantic color role tokens with contrast requirements (4.5:1 normal, 3:1 large/non-text); 4 canonical page templates (List+Detail/Dashboard/Form Workflow/Data Table); 4 breakpoints (sm/md/lg/xl); density modes (comfortable/compact); 7 component patterns (card/table/form/modal/badge/tabs/sidebar) each with full state list; 3 empty state types; 4 error levels; ≤2-step interaction rule for core CRM actions (table of 7 core actions); 6 security-aligned design rules; capability-to-pattern surface mapping table. Self-QC 10/10. | Foundational design system contract; ≤2-step interaction rule is a hard design constraint derived from Pakistan field-sales context; security-aligned design rules prevent auth state from leaking into UI; surface mapping table ensures every CRM capability has a defined component pattern; all component code must consume these tokens, never raw hex or one-off values. |
| `ui-system.md` | Single-page UI system contract covering 19 CRM surfaces; 3-level token architecture (foundation/semantic/domain_alias — e.g., token.stage.closed_won, token.sla.breached, token.billing.delinquent); 8 interaction state contract (default through policy_requires_approval); multi-pane responsive behavior (desktop/tablet/mobile/density); dashboard architecture (5 zones) + 13 dashboard catalog entries; role-gated dashboard defaults per persona (Executor/Manager/Operator/Auditor); 4 cross-dashboard consistency rules; 5-step permission evaluation sequence; 16 workspace models (§5.1–§5.16) each with primary jobs, layout, core modules, and P0 actions; global navigation map (6 groups: Operate/Grow/Revenue/Govern/Build/Observe); workspace data model binding requirements; DesignToken/ComponentContract/DesignSystemSnapshot entity model. Self-QC 10/10. | Master UI system spec; any frontend page that deviates from a workspace model defined here is out of spec; policy_requires_approval state is required for any governed mutation; workspace models define the P0 actions that must be directly accessible on each surface; global navigation map defines the information architecture of the entire application. |

### QC records

| File | Description | Purpose |
|---|---|---|
| `qc-intelligence-data.md` | B4 intelligence/automation layer QC validation record: 10-check matrix covering reporting dashboards, workflow engine, rule engine, AI scoring, predictive models, Customer 360 CDP, event bus; all 10 checks PASS; re-check commands with actual output (21 tests + 6 self-QC scripts → all 10/10, 38 total tests passing). | Proves the B4 intelligence layer was validated before shipment; not a spec; a quality gate record; documents the precise test commands and their results for audit purposes; required reading before any Phase 4 work that touches B4 modules. |
| `qc-integration.md` | B5 external APIs/webhooks and communication integrations QC validation record: 10-check matrix covering provider allowlist, outbound API endpoint alignment, webhook contract alignment, event-to-webhook mapping validity, API standards conformance; all 10 checks PASS; re-check commands with actual output (10 tests + self-QC script → 10/10). | Proves the B5 integration layer was validated; not a spec; a quality gate record; documents the precise test commands for audit purposes; required reading before any Phase 4 work that touches integration modules. |
| `enterprise-depth.md` | Enterprise lifecycle design artifact: 8-stage lifecycle (Policy/Setup → Demand/Attribution → Commercial CPQ → Commitment/Booking → Fulfillment/Service → Escalation/Recovery → Financial Settlement → Retention/Audit/Improvement); CPQ rules — 5 rule families (configuration/pricing/discount/commercial-term/tax), deterministic evaluation contract with ordered precedence, immutable evaluation artifact (rule_set_version, evaluation_id, decision, reasons[]); SLA escalation — 3 states (healthy/at_risk/breached), idempotent escalation actions; 6 governance planes; AI scoring entities (ScoringFactor/LeadScoringInput/OpportunityScoringInput/ScoringResult); predictive model entities (WinProbabilityPrediction/ChurnPrediction/CustomerLifetimeValuePrediction); AI Copilot entities (CopilotContext/CopilotSuggestion/CopilotSuggestionResult, advisory-only invariant); rule engine entities (ConditionRule/ConditionGroup/ActionDefinition/RuleDefinition/RuleEvaluationResult); cross-domain gap closure matrix. Self-QC 10/10. | Closes enterprise-depth gaps in CPQ, partner, SLA, and governance; defines AI/intelligence entity models for code generation; cross-domain gap closure matrix maps each historical gap to a closure mechanism and evidence artifact; NOT absorbed into BACKEND-QC.md — this is a design and entity-model artifact, not a QC pass record. |

---

## §G — backend/docs/adr/ (3 files)

Architectural Decision Records — immutable records of significant architecture choices. Each ADR is permanent; superseded decisions are marked Superseded, not deleted.

| File | Description | Purpose | Status |
|---|---|---|---|
| `ADR-001.md` | DDD + Microservices architecture decision: rationale for domain-driven design over monolith; L1/L2/L3 three-layer model (Core/Interfaces/Adapters); 6 platform engines handling cross-domain concerns; country isolation rule (core/ must not import adapters/pakistan/); ruff CI enforcement of import boundaries; trade-offs (higher complexity vs independent scaling + zero-touch country extensibility). | Records why DDD was chosen and what constraints it imposes; any proposal to merge services or share databases must reference this ADR; immutable historical record of the foundational architectural choice. | Accepted |
| `ADR-002.md` | Adapter Pattern for Pakistan Market Isolation: decision to isolate all country-specific logic in adapters/pakistan/; 5 L2 interface contracts (MessagingAdapter, PaymentAdapter, ComplianceAdapter, PhoneFormatter, LocaleAdapter) with enforcement via ruff import boundary check; JazzCash stub mode default (C-009) rationale; extensibility model for future countries. | Records why the adapter pattern was chosen and how country isolation is enforced; ruff import check is the automated enforcement mechanism; any new Pakistan-specific integration must follow this pattern; rationale for C-009 stub mode default. | Accepted |
| `ADR-003.md` | WhatsApp-First Interaction Model: decision that WhatsApp is the primary execution surface, not an add-on channel; inbound messages trigger system actions; no core workflow requires the browser UI; rationale from Pakistan market context (field sales, low CRM UI literacy). | Records why WhatsApp is primary (not supplementary); constrains product decisions that would make features only accessible via browser UI; informs the anti-lead-loss guarantee design and the activation model's sandbox-first approach. | Accepted |

---

## §H — backend/docs/ — Phase 4 Sprint 0: Design Docs (Sprint 0 complete — all 9 Active)

These docs were identified by the product-spec-gap-register.md (PS-001–PS-010, 2026-05-18). Each must be created in Phase 4 Sprint 0 and added to this catalogue on the same day. Until a doc is created and catalogued, it cannot serve as an audit anchor for code review. **PS-001 cases-domain.md + PS-005 localization.md + PS-008 territory-management.md written 2026-05-19. All 3 Phase-5 build blockers now Active.**

**Phase 5B additions:** 3 further domain specs written 2026-05-29 to unblock Phase 5B sprints 4, 5, and 7. See §M for full entries: `marketing-campaigns.md`, `partners.md`, `ai-predictive-models.md`.

**Rule:** When any doc below is written, update its Status from PLANNED → Active and fill in the Description column fully.

| File | Gap ID | Description when written | Purpose when written | Status | Phase-5 blocker? |
|---|---|---|---|---|---|
| `backend/docs/domain/cases-domain.md` | PS-001 | Case entity (23 fields: case_id, tenant_id, case_number, subject, status, priority, source, category, contact_id, account_id, lead_id, assigned_to, assigned_team_id, queue_id, sla_tier, SLA due timestamps, first_responded_at, resolved_at, resolution_confirmed_at, closed_at, reopen_count, escalation_level, knowledge_article_ids + OCC version_no); CaseComment entity (comment_type enum: internal_note/customer_reply/resolution/status_change/escalation_note); CaseEscalation entity; SupportQueue entity with 4 routing strategies (round_robin/least_loaded/skill_based/manual); SLAPolicy entity with business-hours calculation (PKT UTC+5, Mon–Sat 09:00–19:00); 7-state state machine (OPEN/ASSIGNED/IN_PROGRESS/WAITING_ON_CUSTOMER/RESOLVED/ESCALATED/CLOSED) with guards (resolution comment required before RESOLVED, resolution_confirmed_at required before CLOSED); 4-level escalation ladder using percentage thresholds of resolution window; customer escalation via "ESCALATE"/"مینیجر سے بات کریں" WhatsApp keywords; KnowledgeArticle entity with suggestion on case creation; 19 API endpoints across cases/knowledge/queues resources; RBAC gate table; 12 event types emitted; 3 scanner jobs (SLA monitor every 5min, auto-close after 7-day WAITING_ON_CUSTOMER, resolution confirm after 48h). | Anchors the Cases domain for both backend implementation and frontend support console build; gates B-05 cases.html, C-05 cases-detail.html, E-01 support-console.html, A-07 support-dashboard.html, I-04 case-new.html, C-12 knowledge-article.html; without this spec no support feature can be correctly implemented; also provides KnowledgeArticle entity which has no other spec. | **Active** | Yes — gates B-05, C-05, E-01, A-07, I-04, C-12 |
| `backend/docs/domain/shared-inbox.md` | PS-002 | Conversation entity extensions (assigned_agent_id, queue_id, assignment_reason, handoff_count); InboxQueue entity (routing_strategy enum: round_robin/least_loaded/claim_first/skill_based, auto_assign flag, team scope); AgentPresence entity (status enum: online/away/busy/offline, max_concurrent, open_conversation_count); ConversationHandoff entity (immutable handoff audit record with from_agent/to_agent/reason/note); auto-assignment pipeline (intent→queue routing, eligibility check, null fallback to pool); claim endpoint with atomic `UPDATE WHERE assigned_agent_id IS NULL` (race condition guard); handoff with ownership transfer and `403` enforcement on unauthorized sends; agent-offline handling (conversations returned to pool after 5-min grace); agent view (my conversations + unassigned pool) vs supervisor view (all + presence board); concurrent conflict resolution rules; 11 API endpoints; 7 event types. | Defines the multi-agent assignment model that extends whatsapp-execution-model.md's per-tenant conversation model to per-agent assignment; required before L-01 inbox.html and L-02 inbox-thread.html can be built; without this spec concurrent agent handling and handoff behavior are undefined. | **Active** | No |
| `backend/docs/adapters/compliance-adapter.md` | PS-003 | `ComplianceAdapter(ABC)` protocol at `adapters/interfaces/compliance_adapter.py` with 5 abstract methods: `verify_consent()` (returns bool, never raises), `record_consent()` (append-only, creates new record on revocation), `anonymize_entity()` (idempotent SHA-256 pseudonymization of PII fields; list of 19 PII fields across Contact/Lead/Message/ActivityEvent/InvoiceSummary/SessionToken), `check_retention_policy()` (returns action_required: none/schedule_anonymization/immediate_delete), `audit_access()` (non-blocking PII read audit); `PakistanComplianceAdapter` at `adapters/pakistan/compliance/`; PDPA 2023 + GDPR regulatory context; WhatsApp consent capture rules (SERVICE_COMMUNICATION auto-granted on inbound; MARKETING requires explicit opt-in; STOP/بند کرو revokes marketing); retention rules table (7-year financial/audit, 5-year contacts, 2-year leads/marketing/messages, 90-day sessions); RTbF handling via ERASE MY DATA keyword; 9 mandatory call sites with service + operation + method; 3 explicit non-call sites; `app.state.compliance_adapter` registration pattern; maps to PENDING.md L-03. | Defines the ComplianceAdapter interface that is the third L2 Interface in the adapter architecture (alongside MessagingAdapter + PaymentAdapter); fills the gap left by pakistan-adapter-architecture.md which defines the other two; required for GDPR/PDPA compliance code to be implementable; PENDING.md L-03 (wire ComplianceAdapter) cannot be done without this spec. | **Active** | No |
| `backend/docs/adapters/conversational-action-spec.md` | PS-004 | 10-command dictionary with trigger keywords (EN + Urdu), required context, action, confirmation requirement, and reply format: QueryOpenInvoices, ReportPayment (±5% amount tolerance, YES/NO confirmation), CloseFollowUp, SnoozeFollowUp (date parsing: tomorrow/next week/Monday/explicit/fallback), DisqualifyLead (confirmation gated), CreateLead, CreateReferral, CreateCase, EscalateCase, ConfirmCaseResolution; context resolution pipeline (phone→contact→lead→invoice→case→followup); explicit entity reference extraction (#INV-/#CAS- patterns); pending_command slot on Conversation entity (5-minute expiry, YES/NO/anything-else handling); error response table (6 conditions — all produce customer reply, silent failure not permitted); RBAC bot user role checked before mutation; ActivityEvent source=conversational_whatsapp on all mutations. | Operationalizes PRODUCT-SPEC.md §1/§5.2 "actions via conversation context"; required before any conversational CRM feature or AI Copilot can be implemented; without this spec the intent classifier labels messages but nothing executes; also the only place where Urdu command keywords are specified. | **Active** | No |
| `backend/docs/product/localization.md` | PS-005 | Framework decision (custom JSON key-value registry + browser Intl; no third-party i18n library); 2 locale identifiers (en-PK / ur-PK with IETF BCP 47 tags); 15-namespace key registry format (`<namespace>.<sub_namespace>.<key>`); registry.js with `loadLocale()` + `t()` + variable interpolation `{{var_name}}` + missing-key fallback; crm-rtl.css architecture (always loaded, .rtl class selector, sidebar/main/header/forms/tables/charts RTL overrides); Noto Nastaliq Urdu font loading (line-height: 2.0 requirement); locale detection hierarchy (localStorage → navigator.language → fallback en-PK); locale toggle placement (login + authenticated header); `localechange` custom event; `formatPKR()` using Intl with South Asian lakh grouping; `formatDate()` with ISO 8601 → DD/MM/YYYY; `formatPKPhone()` with +92 normalization; 8 WhatsApp template message pairs (EN+UR) with variable slots; P-017 gate on all outbound Urdu customer messages; 6 invariants (C-001 enforcement, fallback chain, no hardcoded strings, date storage as UTC ISO 8601). | Gates all 75 frontend pages; CONSTRAINTS.md C-001 (RTL from day 1) cannot be implemented without this doc; defines the crm-rtl.css architecture, the t() function pattern, locale toggle, and Urdu template registry; without this spec every page would require independent RTL decision-making leading to inconsistency. | **Active** | Yes — gates all 75 frontend pages (CONSTRAINTS.md C-001) |
| `backend/docs/domain/employee-performance.md` | PS-006 | 8 KPI definitions with formulas and source events: P-01 Leads Captured, P-02 Follow-up Completion Rate (on-time completions only), P-03 Average First Response Time, P-04 Lead Conversion Rate, P-05 Deals Won, P-06 Daily Activity Count (excludes passive view events), P-07 Overdue Follow-up Count, P-08 Average Deal Cycle Time; 4 period types (today/week/month/rolling_30); EmployeePerformanceRM schema (16 fields + partial unique index); EmployeePerformanceSummary API response shape; aggregation source rules (ActivityEvent + Lead + FollowupTask + Opportunity tables); null handling for division-by-zero and low-sample-count warning (n<3); refresh schedule (every 15min for today, 2hr week, 4hr month, daily rolling_30); RBAC visibility (rep=own only, manager=team, admin=all); 4 API endpoints; 2 events (performance.computed + overdue_threshold_exceeded at >5). | Defines per-rep performance indicators that are referenced by owner-dashboard.md and activity-control-model.md but not specified anywhere; required before the team performance panel in dashboard.html or the individual rep performance page can be built; also defines the data that managers use to evaluate rep accountability in the Pakistan field-sales context. | **Active** | No |
| `backend/docs/domain/territory-management.md` | PS-008 | Territory entity (territory_id, tenant_id, name, description, parent_id for 3-level hierarchy, criteria_type enum: geographic/postal/account_segment/rep_assigned/hybrid, criteria_value JSONB, assigned_reps[], primary_manager, is_default, is_active, routing_priority); TerritoryRule entity (rule_id, rule_type: city/postal_code/region/geo_polygon/account_industry/account_size/account_tier/rep_explicit/custom_field, field, operator, value JSONB — full schema for each rule_type); TerritoryAssignment entity with active-assignment partial unique index; default territory invariant (exactly one per tenant); criteria evaluation pipeline (ordered by routing_priority, AND logic across all rules); 4-strategy rep assignment within territory (round_robin/least_loaded/explicit_rule/single-rep); conflict resolution (priority order → rule specificity → UUID tiebreaker); manual override with sticky flag; territory-scoped dashboard queries (admin=all, manager=subtree, rep=own); TerritoryPerformanceRM read model (13 total fields); 7 API endpoints for territory management + 3 for assignments + 1 performance; 7 event types; 3 scanner jobs (auto-assign every 1min, performance aggregation daily, inactive cleanup weekly); RBAC gate table. | Gates G-09 territories.html; expands the TerritoryRule pointer in domain-model.md into a full implementation contract; required before lead auto-routing, territory-scoped dashboards, or the territories admin UI can be built; defines the territory_ids JWT claim that scopes all rep-level data access. | **Active** | Yes — gates G-09 territories.html |
| `backend/docs/product/pricing-plans.md` | PS-009 | 4 plan tiers (Starter Rs 1,999/Growth Rs 5,999/Business Rs 14,999/Enterprise custom) with PKR benchmarks aligned to PRODUCT-SPEC.md §3/§5; annual discount rates (15%/20%); user seat caps (5/20/unlimited); feature entitlement matrix (21 features across 4 plans); 14 feature→flag_key mappings; `EntitlementGuard` FastAPI dependency pattern with `require_feature()` and `402 PLAN_FEATURE_NOT_ENTITLED` error envelope with upgrade URL; seat cap `402 SEAT_LIMIT_REACHED`; TenantEntitlement entity extensions (plan_code, trial fields, billing_cycle, subscription_status, seat_limit); 14-day Growth trial on tenant creation; auto-downgrade to Starter (not suspension) on trial expiry; trial expiry notifications at T-7/T-3/T-1/T+0; upgrade: immediate + proration; downgrade: deferred to period end + data retained read-only + 14-day grace for seat over-limit; flat-rate v1 metering (usage metering deferred to Phase 6); 5 API endpoints. | Makes PRODUCT-SPEC.md §2/§12 pricing strategy concrete as an implementation contract; required before subscription billing, plan-gated feature enforcement, or the billing-settings.html page can be built; defines the EntitlementGuard pattern that all gated endpoints depend on; without this spec feature flags have no plan context. | **Active** | No |
| `backend/docs/adapters/integration-flow-traces.md` | PS-010 | 4 end-to-end flow traces: (1) WhatsApp→Lead→Follow-up→Close (15 steps: webhook async return → intent classification → lead creation → T+0 follow-up → activity log → opportunity won → follow-up bulk close; end-state: Lead=CLOSED_WON, all tasks COMPLETED, ActivityEvent chain intact); (2) Lead→Invoice→Payment→Reconciliation (11 steps: opportunity.won → invoice created → WhatsApp send → JazzCash callback → confidence scoring → auto-reconcile ≥85/manual 40-84/unmatched <40 → invoice PAID; end-state: Invoice.status=PAID, PaymentEvent confirmed); (3) Follow-up→Escalation→Reassignment (13 steps: overdue scanner → PENDING→OVERDUE → Level 1 notify rep → T+24h Level 3 notify manager → manager reassigns → OVERDUE→PENDING; end-state: task reassigned, escalation resolved, no OVERDUE tasks with no owner); (4) Offline Action→Sync→Consistent State (13 steps: CommandRecord queue → connectivity restored → batch POST → idempotency check → conflict detection → server-wins for Lead → partial batch failure rules → device state reconciled; end-state: lead on server, idempotency key in ledger, no duplicates, user notified on failure); 6 cross-flow invariants (webhook 200 async, all mutations idempotent, ActivityEvent chain always complete, no silent data loss, OCC on all aggregates, tenant_id isolation). | Applies execution-hardening.md + workflow-catalog.md rules to 4 concrete cross-service scenarios; required for integration testing spec: each end-state assertion is a test assertion; without this doc integration tests have no authoritative expected outcomes to verify against; also validates that all 4 PRODUCT-SPEC.md §1/§12 mandatory flows are fully specced. | **Active** | No |

**Note — PS-007 (DONE 2026-05-19):** `backend/docs/domain/collections-engine-model.md` extended with §N — Manual Payment Proof workflow. Existing §F catalogue entry updated above to reflect the new section.

| `backend/docs/_qc/phase4-stage1-read-log.md` | — | Per-file read status tracker for all 51 §F + §H specs; status key: ⬜ not read / ✓ M read by main session; per-row fields: file path, line count, status, and structured findings; 30 duplication/overlap clusters logged with PRIMARY source designations; resume point updated after each file to survive session compression; final state: 51 ✓ / 0 ⬜ (Stage 1 COMPLETE 2026-05-23). | Provides cross-session continuity for Stage 1 doc normalisation; proves every spec was read line-by-line by the main session (not agent-delegated); structured findings are the authoritative input to Stage 2 Code Overlay; must be updated whenever new specs are added to §F or §H. | **Active** | No |
| `backend/docs/phase4-gap-register.md` | — | Phase 4 Stage 3 Code Overlay gap register: 28 gaps across Groups A (persistence), B (security/auth), C (missing domain APIs), D (API standards/state machines), E (observability/CI/CD). Each row has ID, file, description, and OPEN/FIXED status. Fix log at bottom records date, gap ID, and change made. Created 2026-05-25; updated as Stage 3 fixes land. | Living document anchoring Stage 3 work; proves all gaps were audited and tracked; status column gates Phase 4 completion (all OPEN → FIXED before Stage 4 mapping phase begins). | **Active** | No |

---

## §J — Frontend Pages & JS Drivers (2026-05-29 additions)

New files created this session — catalogued on creation per evolution archive rule.

| File | ID | Description |
|------|-----|-------------|
| `frontend/src/app/user-management-crm.html` | G-02 | Admin User Management page. Posture strip (admin role count), 4 KPI cards, DataTable with role/status filter chips, 2-step Invite User wizard modal, Edit Role / Suspend / Reset Password confirm modals. CRM_PAGE=`user-management-crm`. |
| `frontend/src/assets/js/app/crm-user-management-crm.js` | G-02 | Driver for user-management-crm.html. Reads `d.users.data`. KPI population, DataTable init with role/status filter search extension, invite wizard step navigation, edit-role modal, suspend/reset confirm handlers. |
| `frontend/src/app/invoices.html` | B-09 | Invoice Queue list page. 4 KPI cards (Total Invoiced/Outstanding/Paid This Month/Overdue Count), DataTable with status filter chips. CRM_PAGE=`invoices`. |
| `frontend/src/assets/js/app/crm-invoices.js` | B-09 | Driver for invoices.html. Reads `d.invoices.data`. KPIs derived from dataset. DataTable with Balance column (total−paid), overdue dates in red, status filter search extension. |
| `frontend/src/app/subscriptions-detail.html` | C-09 | Subscription Detail entity view. Identity strip with status-gated action buttons (Renew/Suspend/Cancel). MRR/ARR/Cycle KPI strip. 4-tab main pane (Plan Details/Invoice History/Payment Events/Usage). Context panel (Churn Risk/Expansion/Account Health). CRM_PAGE=`subscriptions-detail`. |
| `frontend/src/assets/js/app/crm-subscriptions-detail.js` | C-09 | Driver for subscriptions-detail.html. Demo record sub-001. Reads `d.subscriptions.data`. Status gates buttons per enum. Joins invoices by account_id. Derives payment events from paid invoices. Expansion from opportunities. Account health from contacts. Cancel requires reason entry. |
| `frontend/src/app/subscriptions-dashboard.html` | A-06 | Subscription Revenue Dashboard. Churn-risk posture strip, MRR/ARR/Renewal Rate KPI cards, ApexCharts cohort retention area chart, delinquent accounts queue, active subscriptions table, expansion-vs-churn delta panel. P-016 stub comment. CRM_PAGE=`subscriptions-dashboard`. |
| `frontend/src/assets/js/app/crm-subscriptions-dashboard.js` | A-06 | Driver for subscriptions-dashboard.html. Reads `d.subscriptionKpi` + `d.subscriptions.data`. P-016: JazzCash/Easypaisa section hidden (JAZZCASH_STUB_MODE=true). ApexCharts cohort retention line chart from `kpi.cohort_retention`. Delinquent list = past_due subscriptions. |

**crm-dummy.js additions (first batch, 2026-05-29):**
- `INVOICES` — 10 invoice records. Exposed as `d.invoices`.
- `SUBSCRIPTIONS` — 8 subscription records. Exposed as `d.subscriptions`.
- `SUBSCRIPTION_KPI` — aggregate KPI object. Exposed as `d.subscriptionKpi`.

---

## §K — Frontend Pages & JS Drivers (2026-05-29 Cat 1 batch)

Second batch of new files — all 15 remaining Cat 1 pages. Catalogued on creation per evolution archive rule.

| File | ID | Description |
|------|-----|-------------|
| `frontend/src/app/tenants-dashboard.html` | A-11 | Tenant & Entitlement Dashboard. Plan/seat/feature KPI cards, entitlements-at-limit execution queue, tenant summary. CRM_PAGE=`tenants-dashboard`. |
| `frontend/src/assets/js/app/crm-tenants-dashboard.js` | A-11 | Reads `d.tenantKpi` + `d.featureFlags`. Populates KPIs, near-limit feature list. |
| `frontend/src/app/accounts.html` | B-04 | Account List. Tier/Industry/City/Open Opps/Outstanding Balance columns. Tier + balance filter chips. CRM_PAGE=`accounts`. |
| `frontend/src/assets/js/app/crm-accounts.js` | B-04 | Reads `d.accounts.data`. DataTable with tier + balance filter search extension. Outstanding balance in red. |
| `frontend/src/app/accounts-detail.html` | C-03 | Account Profile. Identity strip, 4-tab main pane (Details/Contacts/Opportunities/Invoices), account health context panel. CRM_PAGE=`accounts-detail`. |
| `frontend/src/assets/js/app/crm-accounts-detail.js` | C-03 | Demo a-002 (City Pharma Ltd). Joins contacts/opps/invoices by account_id. Context: open opps count, outstanding balance, contact count. |
| `frontend/src/app/orders-detail.html` | C-07 | Order Detail. Immutable-after-activation badge, 3-tab pane (Line Items/Addresses/Invoices), fulfilment status, linked quote. CRM_PAGE=`orders-detail`. |
| `frontend/src/assets/js/app/crm-orders-detail.js` | C-07 | Demo ord-001 (City Pharma Ltd). Reads `d.orders.data`. Immutable UI — no edit controls shown. Joins invoices + quotes by FK. |
| `frontend/src/app/invoices-detail.html` | C-08 | Invoice Detail. Total/Paid/Balance KPI strip, 3-tab pane (Details/Payment History/Proof Attachments), reconciliation status, linked opportunity. CRM_PAGE=`invoices-detail`. |
| `frontend/src/assets/js/app/crm-invoices-detail.js` | C-08 | Demo i-001. Reads `d.invoices.data`. Record-payment button hidden when status=paid. Reconciliation status derived from invoice status. |
| `frontend/src/app/contact-new.html` | I-02 | New Contact Form. 2-step wizard (Identity → Account & Tags), phone dedup warn on blur, multi-toggle tag selector, discard modal. CRM_PAGE=`contact-new`. |
| `frontend/src/assets/js/app/crm-contact-new.js` | I-02 | Reads `d.contacts.data` for dedup + `d.accounts.data` for dropdown. Step navigation, validation, tag toggle, success screen. |
| `frontend/src/app/org-settings.html` | G-01 | Organization Settings. Identity/Locale/Currency/Business Hours editor. Shared settings `list-group` left-nav linking all G-pages. CRM_PAGE=`org-settings`. |
| `frontend/src/assets/js/app/crm-org-settings.js` | G-01 | Save button feedback only (no API call in dummy mode). |
| `frontend/src/app/roles.html` | G-03 | Role & Permission Editor. Roles table from `d.roles.data`, permission registry read-only badge list. CRM_PAGE=`roles`. |
| `frontend/src/assets/js/app/crm-roles.js` | G-03 | Reads `d.roles.data`. Renders role table with delete guard (user_count > 0). Permission registry built from union of all role permissions. |
| `frontend/src/app/billing-settings.html` | G-04 | Billing & Subscription Settings. Plan/seats/renewal from `d.tenantKpi`. Payment method P-016 stub warning. Invoice history stub. CRM_PAGE=`billing-settings`. |
| `frontend/src/assets/js/app/crm-billing-settings.js` | G-04 | Reads `d.tenantKpi`. P-016: JazzCash/Easypaisa section not shown. Stub invoice history rows hardcoded (billing-specific, not domain invoices). |
| `frontend/src/app/integrations.html` | G-05 | Integration Settings. WhatsApp/Email/SMS/Push config panels. Credentials masked last-4. Test-connection button with spinner feedback. CRM_PAGE=`integrations`. |
| `frontend/src/assets/js/app/crm-integrations.js` | G-05 | Test connection button: spinner → "Connected" feedback after 1.2s delay (dummy mode simulation). |
| `frontend/src/app/notifications.html` | G-06 | Notification Settings. Per-event toggle table (In-App/Email/WhatsApp/SMS for 8 event types). Quiet hours time config. CRM_PAGE=`notifications`. |
| `frontend/src/assets/js/app/crm-notifications.js` | G-06 | EVENTS config array drives toggle table rendering. All toggles live (Bootstrap form-switch). |
| `frontend/src/app/feature-flags.html` | G-07 | Feature Flags. Flag registry from `d.featureFlags`. All toggles require 2-person approval modal (per spec §2.4). Category + rule_type badges. CRM_PAGE=`feature-flags`. |
| `frontend/src/assets/js/app/crm-feature-flags.js` | G-07 | Reads `d.featureFlags.data`. Toggle change reverted until confirm modal approved. Pending toggle state tracked in closure. |
| `frontend/src/app/compliance.html` | G-08 | Compliance Settings. Retention policy editor, data governance console link, break-glass log (read-only stub). CRM_PAGE=`compliance-settings`. |
| `frontend/src/assets/js/app/crm-compliance-settings.js` | G-08 | Stub driver — no data reads (all content static per spec: retention periods are configuration, not CRM_DUMMY). |
| `frontend/src/app/data-governance.html` | J-03 | Data Governance Console. 4-tab: Data Classification (PII/financial/internal map) / Retention Policies / Subject Access Requests / Consent Management. CRM_PAGE=`data-governance`. |
| `frontend/src/assets/js/app/crm-data-governance.js` | J-03 | Classification + retention from spec constants. Consent summary from `d.contacts.data`. SAR count = 0 (stub). |
| `frontend/src/app/privacy.html` | J-05 | Consent & Privacy Manager. 3-tab: Consent Records / Data Subject Requests / Deletion Workflows. GDPR/PDPA 2023 aligned. CRM_PAGE=`privacy`. |
| `frontend/src/assets/js/app/crm-privacy.js` | J-05 | Consent records from `d.contacts.data` tags. Erasure form validates contact + reason before submit. 30-day SLA clock message on submit. |

**crm-dummy.js additions (second batch, 2026-05-29):**
- `ACCOUNTS` — 12 account records: account_id, name, tier (SMB/Mid-Market/Enterprise), industry, owner_id, city, open_opps, outstanding_balance. Exposed as `d.accounts`.
- `ORDERS` — 2 order records: order_id, order_number, account_id, account_name, status (activated/draft), total_amount, line_items[], billing_address, shipping_address, quote_id, fulfillment_status, invoice_ids[]. Exposed as `d.orders`.
- `TENANT_KPI` — object: plan_tier, seat_count, seat_limit, enabled_feature_count, entitlement_limit, entitlements_at_limit, entitlement_overage_count, renewal_date, active_sessions. Exposed as `d.tenantKpi`.
- `FEATURE_FLAGS` — 6 flag records: flag_key, label, enabled, category, description, rule_type. Exposed as `d.featureFlags`.
- `ROLES` — 4 role records: role_id, name, user_count, is_system, permissions[]. Exposed as `d.roles`.

---

## §L — Frontend Pages & JS Drivers (2026-05-29 Cat 2 batch)

All 28 Cat 2 pages — browser-approved 2026-05-29. **All backend domains built (Phase 5B complete 2026-05-30)** + **all 28 wired to live API (Phase 6 wiring complete 2026-05-31).** A-08 wired via v1-communications.routes.js inline stub. DB wiring (replace in-memory stores with PostgreSQL) is commercialization-phase task.

| File | ID | Description |
|------|-----|-------------|
| `frontend/src/app/support-dashboard.html` + `crm-support-dashboard.js` | A-07 | Case SLA Operations Dashboard. Posture strip (breach count), 4 KPI cards (Open Cases/Avg First Response/SLA Breach Rate/At-Risk), dt_SlaQueue DataTable (at-risk/breached only), ApexCharts area chart (case volume trend). Reads `d.cases` + `d.caseSlaKpi`. CRM_PAGE=`support-dashboard`. |
| `frontend/src/app/engagement-dashboard.html` + `crm-engagement-dashboard.js` | A-08 | Engagement & Comms Dashboard. 4 KPI cards (Delivery Rate/Open Rate/Reply Rate/Failed), active campaigns list-group queue, ApexCharts grouped bar (channel engagement rates). Reads `d.campaigns` + `d.commsKpi`. CRM_PAGE=`engagement-dashboard`. |
| `frontend/src/app/knowledge-dashboard.html` + `crm-knowledge-dashboard.js` | A-09 | Knowledge Base Dashboard. 4 KPI cards (Published/Deflection Rate/Stale/Zero-View), stale+zero-view article queue, ApexCharts area chart (adoption trend). Reads `d.knowledgeArticles` + `d.knowledgeKpi`. CRM_PAGE=`knowledge-dashboard`. |
| `frontend/src/app/workflows-dashboard.html` + `crm-workflows-dashboard.js` | A-10 | Workflow Automation Dashboard. Posture strip (failure count), 4 KPI cards (Volume/Success Rate/Retry Depth/Failed), failed/retrying queue, ApexCharts grouped bar (pass/fail trend). Reads `d.workflowExecutions` + `d.workflowKpi`. CRM_PAGE=`workflows-dashboard`. |
| `frontend/src/app/cases.html` + `crm-cases.js` | B-05 | Case Queue. 4 KPI cards (Open/SLA Breached/At Risk/Resolved), dt_Cases DataTable, dual filter chips (Status × SLA), cross-filter logic via dt.rows().every(). CRM_PAGE=`cases`. |
| `frontend/src/app/partners.html` + `crm-partners.js` | B-11 | Partner List. 4 KPI cards (Total/Active/Open Opps/Commission Due PKR), dt_Partners DataTable, Tier × Status filter chips, PKR commission column. CRM_PAGE=`partners`. |
| `frontend/src/app/cases-detail.html` + `crm-cases-detail.js` | C-05 | Case Detail. SLA timer identity strip (color-coded by sla_state), state-gated Claim/Resolve buttons, 3-tab pane (Conversation/Fields/Resolution), escalation controls by sla_state. Demo cs-001. CRM_PAGE=`cases-detail`. |
| `frontend/src/app/workflow-run-detail.html` + `crm-workflow-run-detail.js` | C-10 | Workflow Run Detail. Execution identity strip, state-gated Retry button (failed only), 3-tab pane (Log/Steps/Error Details), retry context panel. Demo exec-007 (failed). CRM_PAGE=`workflow-run-detail`. |
| `frontend/src/app/partners-detail.html` + `crm-partners-detail.js` | C-11 | Partner Profile. Identity strip, 4-tab pane (Details/Opportunities/Commission Ledger/Relationship History), attribution summary + commission context panel. Demo p-001 (NovaTech, Platinum). CRM_PAGE=`partners-detail`. |
| `frontend/src/app/knowledge-article.html` + `crm-knowledge-article.js` | C-12 | Knowledge Article Detail. State-gated Publish/Edit buttons, 4-tab pane (Content/Version History/Related Articles/Feedback), article stats context panel. Demo art-001. CRM_PAGE=`knowledge-article`. |
| `frontend/src/app/support-console.html` + `crm-support-console.js` | E-01 | Support Console. 3-pane layout (SLA queue col-lg-3 / conversation thread col-lg-5 / context+escalation col-lg-4), global SLA timer in header color-coded by top queue item sla_state, click-to-select queue. CRM_PAGE=`support-console`. |
| `frontend/src/app/marketing-workspace.html` + `crm-marketing-workspace.js` | F-01 | Marketing Workspace. 4 KPI cards, dt_Campaigns DataTable, Status filter chips, TYPE/STATUS badges. Reads `d.campaigns`. CRM_PAGE=`marketing-workspace`. |
| `frontend/src/app/territories.html` + `crm-territories.js` | G-09 | Territory Management (settings two-pane). list-group left-nav, territory tree table, rule editor (Punjab North demo rules), assignment strategy section. Reads `d.territories`. CRM_PAGE=`territories`. |
| `frontend/src/app/marketing-analytics.html` + `crm-marketing-analytics.js` | H-02 | Marketing Analytics. flatpickr date range, 4 KPI cards, ApexCharts grouped bar (channel engagement rates), ApexCharts area (WhatsApp opt-in trend), dt_MarketingCampaigns DataTable. CRM_PAGE=`marketing-analytics`. |
| `frontend/src/app/support-analytics.html` + `crm-support-analytics.js` | H-03 | Support Analytics. flatpickr date range, 4 KPI cards, ApexCharts line (SLA breach trend), ApexCharts donut (case volume by source), dt_SupportCases DataTable. CRM_PAGE=`support-analytics`. |
| `frontend/src/app/workflow-analytics.html` + `crm-workflow-analytics.js` | H-05 | Workflow Analytics. flatpickr date range, 4 KPI cards, ApexCharts grouped bar (pass/fail trend), ApexCharts horizontal bar (failure rate by workflow), dt_WorkflowExec DataTable. CRM_PAGE=`workflow-analytics`. |
| `frontend/src/app/report-builder.html` + `crm-report-builder.js` | H-07 | Custom Report Builder. 4-step wizard (metric selector checkboxes / group-by dropdown / chart type picker / save+schedule form), live ApexCharts preview panel that re-renders on metric/chart-type change. CRM_PAGE=`report-builder`. |
| `frontend/src/app/case-new.html` + `crm-case-new.js` | I-04 | New Case Form (2-step wizard). Step 1: contact live search (blur → d.contacts match) + subject + priority. Step 2: queue + category + description. Dummy success toast → redirect cases.html. CRM_PAGE=`case-new`. |
| `frontend/src/app/campaign-new.html` + `crm-campaign-new.js` | I-06 | New Campaign Wizard (2-step). P-017 alert. Step 1: name + segment chip buttons + type. Step 2: template selector + trigger + flatpickr schedule. Dummy success toast. CRM_PAGE=`campaign-new`. |
| `frontend/src/app/workflow-builder.html` + `crm-workflow-builder.js` | K-01 | Workflow Builder. 3-pane: left palette (authoring primitives) / center canvas (simulated node graph: Bootstrap cards + CSS dashed connectors) / right inspector (form for selected node). Validate/Simulate/Save Draft/Publish buttons. Problems panel. CRM_PAGE=`workflow-builder`. |
| `frontend/src/app/object-builder.html` + `crm-object-builder.js` | K-02 | Object Builder. Object type selector (Lead/Contact/Opp/Account/Custom), field list from OBJECT_FIELDS config (10 Lead fields), layout canvas (4 dashed sections), layout preview (rendered form). CRM_PAGE=`object-builder`. |
| `frontend/src/app/rule-builder.html` + `crm-rule-builder.js` | K-03 | Rule Builder. Dynamic condition rows (field + operator + value, add/remove), dynamic action rows (type + value, add/remove), pre-seeded discount_approval_routing rule, Test Rule button with sample context evaluation. CRM_PAGE=`rule-builder`. |
| `frontend/src/app/approval-lanes.html` + `crm-approval-lanes.js` | K-04 | Approval Lanes Kanban. 4-lane Bootstrap grid (Draft/Pending/Approved/Rejected), quote cards populated from `d.quotes.data` filtered by status, calcTotal() per-line discount math, discount>10% badge. CRM_PAGE=`approval-lanes`. |
| `frontend/src/app/inbox.html` + `crm-inbox.js` | L-01 | Shared Inbox. Channel selector chips (All/WhatsApp/Email/SMS), two-pane (thread list left / thread view right), thread-item CSS classes + unread indicator + intent badges, auto-select first thread, relativeTime() helper, search + channel filter. Reads `d.messageThreads`. CRM_PAGE=`inbox`. |
| `frontend/src/app/inbox-thread.html` + `crm-inbox-thread.js` | L-02 | Inbox Thread View. Customer context strip (channel icon/agent/Reassign/Close), WhatsApp-style msg-in/msg-out bubbles with delivery ticks, context panel: customer data + intent badge + confidence + INTENT_ACTIONS suggested CTAs (2 per intent type). Demo th-001. CRM_PAGE=`inbox-thread`. |
| `frontend/src/app/routing-config.html` + `crm-routing-config.js` | L-03 | Routing Configuration (settings two-pane). Queue Management table (4 queues), Agent Capacity table (d.users + PRESENCE map), Routing Rules priority list (3 rules), Fallback Config section. CRM_PAGE=`routing-config`. |
| `frontend/src/app/ai-copilot.html` + `crm-ai-copilot.js` | M-01 | AI Copilot. Advisory-only banner + badge. Lead score card (82/100, score drivers + progress bars). Next-action suggestion card ([Take Action]+[Dismiss]). Risk flags panel (live from d.overdueFollowups + d.cases). Conversational CRM: intent classifier (classifyIntent regex), INTENT_RESPONSES map, chat history, Enter key support. CRM_PAGE=`ai-copilot`. |
| `frontend/src/app/ai-insights.html` + `crm-ai-insights.js` | M-02 | AI Insights Dashboard. 4 KPI cards (Avg Win Probability/Churn Risk Accounts/Avg CLV/Model Accuracy), ApexCharts bar (win prob distribution by bucket), ApexCharts donut (churn risk: High/Medium/Low), ApexCharts bar (CLV by account), Feature Weight Inspector (7 features with progress bars). Derived from `d.opportunities`. CRM_PAGE=`ai-insights`. |

**crm-dummy.js additions (Cat 2 batch, 2026-05-29):**
- `CASES` — 10 case records: case_id, case_number, subject, status (CaseStatus enum: OPEN/ASSIGNED/IN_PROGRESS/WAITING_ON_CUSTOMER/RESOLVED/ESCALATED/CLOSED), priority, sla_state (healthy/at_risk/breached), response_due_at, sla_resolution_due_at, queue, category, contact + account FK fields. Exposed as `d.cases`.
- `CASE_SLA_KPI` — aggregate: sla_breach_count, open_case_count, avg_first_response_minutes, breach_rate, at_risk_case_count, unacknowledged_breach_count. Exposed as `d.caseSlaKpi`.
- `PARTNERS` — 8 partner records: partner_id, name, partner_tier (Platinum/Gold/Silver), region, city, status, attributed_opp_count, commission_due, contact fields. Exposed as `d.partners`.
- `CAMPAIGNS` — 6 campaign records: campaign_id, name, type (whatsapp_broadcast/email/sms), status, segment_name, reach, delivery_rate, open_rate, reply_rate, leads_generated, conversions. Exposed as `d.campaigns`.
- `COMMS_KPI` — aggregate: delivery_rate, open_rate, reply_rate, failed_delivery_count, whatsapp_opted_in, whatsapp_opt_out_rate. Exposed as `d.commsKpi`.
- `KNOWLEDGE_ARTICLES` — 8 article records: article_id, title, category (getting_started/billing/how_to/technical), status (published/draft/archived), view_count, deflection_count, last_updated_at, stale (bool). Exposed as `d.knowledgeArticles`.
- `KNOWLEDGE_KPI` — aggregate: published_article_count, case_deflection_rate, stale_article_count, zero_view_article_count, article_adoption_trend (6-month array). Exposed as `d.knowledgeKpi`.
- `WORKFLOW_EXECUTIONS` — 8 execution records: execution_id, workflow_key, workflow_name, status (succeeded/failed/retrying/running), trigger_event, started_at, ended_at, duration_ms, step_count, failed_step. Exposed as `d.workflowExecutions`.
- `WORKFLOW_KPI` — aggregate: failure_count, execution_volume, success_rate, retry_queue_depth, escalation_count, execution_trend (5-day array). Exposed as `d.workflowKpi`.
- `MESSAGE_THREADS` — 8 thread records: thread_id, contact_name, contact_phone, channel (whatsapp/email/sms), last_message_preview, last_message_at, unread_count, assigned_agent_id, queue_id, status (open/resolved), intent. Exposed as `d.messageThreads`.
- `TERRITORIES` — 4 territory records: territory_id, name, criteria_type, primary_manager, assigned_reps[], parent_id, routing_priority, is_default. Exposed as `d.territories`.

**crm-custom.css additions (Cat 2 batch, 2026-05-29):**
- Global ApexCharts overflow fix: `.apexcharts-canvas { max-width:100% !important; overflow:hidden !important; }` + `.apexcharts-canvas > svg { max-width:100% !important; }` — prevents chart SVG from overflowing Bootstrap flex columns.
- DataTable body-alignment rules (with !important): dt_SlaQueue, dt_Cases, dt_Partners, dt_MarketingCampaigns, dt_SupportCases, dt_WorkflowExec, dt_Campaigns.

---

## §M — Domain Specs Written for Phase 5B (2026-05-29)

Three new domain spec docs written before Phase 5B sprints 4, 5, and 7 can begin. Format follows `cases-domain.md` pattern: ownership block, purpose, core principles, entity model, state machine, business logic, API endpoints, RBAC, events, scanner jobs, acceptance checklist.

| File | Sprint | Description |
|------|--------|-------------|
| `backend/docs/domain/marketing-campaigns.md` | 5B-4 | Campaign lifecycle (draft→active→completed), CampaignSegment rule engine, MessageTemplate contract, dispatch rate limiting (80 msg/min WhatsApp), delivery/read/reply tracking, WhatsApp opt-out handling, conversion attribution (last-touch, 30-day window). P-017 Urdu approval gate baked in. Entities: Campaign, CampaignSegment, MessageTemplate, CampaignSend, CampaignConversion. 11 campaign endpoints + 5 segment endpoints + 4 template endpoints. |
| `backend/docs/domain/partners.md` | 5B-5 | Partner tier system (Platinum 15% / Gold 10% / Silver 5%), DealRegistration protection window, PartnerCommission lifecycle (pending→approved→paid), opportunity attribution (exclusive, last-write), commission immutability on paid status. Entities: Partner, DealRegistration, PartnerCommission, PartnerActivityLog. 9 partner endpoints + 4 deal-registration endpoints + 2 attribution endpoints. |
| `backend/docs/domain/ai-predictive-models.md` | 5B-7 | Advisory-only architecture (no auto-action), 3 v1 rule-based models: lead_score_v1 (9-feature weighted sum, 0–100), churn_predict_v1 (risk factor accumulation → probability), clv_estimate_v1 (avg_monthly_revenue × horizon × retention). CopilotSuggestion entity (6 types, evidence_anchor required). Conversational query intent classifier (5 classes, rule-based). 5 scanner jobs. Full model registry (read-only). Entities: LeadScore, ChurnPrediction, CLVEstimate, CopilotSuggestion, ScoringModel, FeatureContribution. |

---

## §N — Phase 5B Backend Code Files (2026-05-30)

New non-.md files created in Phase 5B sprints — catalogued per evolution archive rule. All files write-only in production (no direct caller except gateway layer and test suite).

### ORM Models (`services/db/models/`)

| File | Domain | Models |
|------|--------|--------|
| `cases.py` | Cases / Support | Case, CaseComment, CaseEscalation, SupportQueue, SLAPolicy, KnowledgeArticle |
| `inbox.py` | Shared Inbox | InboxQueue, AgentPresence, ConversationHandoff |
| `territories.py` | Territories | Territory, TerritoryRule, TerritoryAssignment |
| `campaigns.py` | Marketing | Campaign, CampaignSegment, MessageTemplate, CampaignSend, CampaignConversion |
| `partners.py` | Partners | Partner, DealRegistration, PartnerCommission, PartnerActivityLog |
| `workflows.py` | Workflows | WorkflowDefinition, WorkflowExecution, WorkflowStep |
| `ai_scores.py` | AI / Predictive | LeadScore, ChurnPrediction, CLVEstimate, CopilotSuggestion |

### Alembic Migrations (`alembic/versions/`)

| File | Chain position | Tables |
|------|---------------|--------|
| `0004_cases_schema.py` | 0003 → 0004 | cases, case_comments, case_escalations, support_queues, sla_policies, knowledge_articles |
| `0005_inbox_schema.py` | 0004 → 0005 | Extends conversations + inbox_queues, agent_presence, conversation_handoffs |
| `0006_territories_schema.py` | 0005 → 0006 | territories, territory_rules, territory_assignments (2 partial unique indexes) |
| `0007_campaigns_schema.py` | 0006 → 0007 | campaigns, campaign_segments, message_templates, campaign_sends, campaign_conversions |
| `0008_partners_schema.py` | 0007 → 0008 | partners, deal_registrations, partner_commissions, partner_activity_logs |
| `0009_workflows_schema.py` | 0008 → 0009 | workflow_definitions, workflow_executions, workflow_steps (unique workflow_key per tenant) |
| `0010_ai_scores_schema.py` | 0009 → 0010 | lead_scores, churn_predictions, clv_estimates, copilot_suggestions — **chain sealed** |

### Service Packages (`services/`)

| Package | Files | Key exports |
|---------|-------|-------------|
| `services/cases/` | `__init__.py`, `entities.py`, `service.py` | ALLOWED_TRANSITIONS, CaseTransitionError, compute_sla_deadline(), CasesService |
| `services/inbox/` | `__init__.py`, `entities.py`, `service.py` | PresenceStatus, INTENT_QUEUE_MAP, InboxService, auto_assign(), validate_claim() |
| `services/territories/` | `__init__.py`, `entities.py`, `service.py` | evaluate_rule() (9 rule types), TerritoryService, resolve_conflict() |
| `services/campaigns/` | `__init__.py`, `entities.py`, `service.py` | ALLOWED_TRANSITIONS, validate_activation_guards() (P-017 guard), CampaignService |
| `services/partners/` | `__init__.py`, `entities.py`, `service.py` | COMMISSION_RATES, validate_commission_transition() (paid immutable), PartnerService |
| `services/workflows/` | `__init__.py`, `entities.py`, `service.py` | validate_steps_dsl(), CATALOG_WORKFLOWS (5 system workflows), WorkflowService |
| `services/ai/` | `__init__.py`, `entities.py`, `service.py` | compute_lead_score() (9-feature), classify_query_intent() (5-class), SCORING_MODELS, AIService |

### Gateway Routes (`gateway/routes/`)

| File | Endpoints | Key design notes |
|------|-----------|-----------------|
| `v1-cases.routes.js` | 9 (casesRouter) + 3 (supportRouter) | Seeded 8 cases; SLA state derived from timestamps; resolution comment guard |
| `v1-knowledge.routes.js` | 5 (knowledgeRouter) | Seeded 6 articles |
| `v1-inbox.routes.js` | 11 | Claim guard: assigned_agent_id IS NULL; handoff: supervisor-only |
| `v1-territories.routes.js` | 11 | Dry-run evaluate; manual override creates superseded_by chain |
| `v1-campaigns.routes.js` | 10 | P-017 Urdu gate on activate; WhatsApp opt-in check on send |
| `v1-segments.routes.js` | 5 | Segment validate returns estimated_size + sample |
| `v1-templates.routes.js` | 4 | Template meta_template_status approval check |
| `v1-partners.routes.js` | 13 (partnersRouter + dealRegsRouter) | 409 on paid commission edit; commission immutability enforced at route layer |
| `v1-workflows.routes.js` | 10 | `/runs/*` declared before `/:id` (collision guard); retry creates new execution |
| `v1-ai.routes.js` | 13 | Advisory-only; evidence_anchor required on all suggestions; intent classifier (5 classes) |

### Tests (`tests/`)

| Directory | Unit tests | Integration tests | Domain |
|-----------|-----------|------------------|--------|
| `tests/cases/` | 14 | 15 | Cases state machine + API |
| `tests/inbox/` | 18 | 16 | Inbox assignment + routing API |
| `tests/territories/` | 20 | 16 | Territory rule evaluation + API |
| `tests/campaigns/` | 22 | 18 | Campaign lifecycle + P-017 guard + API |
| `tests/partners/` | 22 | 18 | Commission lifecycle + immutability + API |
| `tests/workflows/` | 26 | 19 | Workflow DSL validation + execution + retry + API |
| `tests/ai/` | 28 | 19 | Scoring formulas + intent classifier + suggestion guard + API |
| **Total Phase 5B** | **150** | **121** | **271 new tests** |

---

## §I — Non-Negotiables and Permanently Blocked Items

Quick-reference tables originally maintained in DOC-CATALOGUE-OPS.md (merged and deleted 2026-05-22). These rules and blocks apply to every contribution, every session, without exception.

### Permanently Blocked Items

These items are flagged across multiple docs. Never attempt to unblock or implement without the prerequisite external action.

| ID | Item | Blocked by | Blocked in |
|---|---|---|---|
| P-016 | JazzCash/Easypaisa production payment callbacks | Real credentials + provider sandbox verification + full payment→webhook→reconciliation cycle E2E test | CONSTRAINTS.md C-009 · backend/PENDING.md · FRONTEND-BACKEND-MAPPING.md §20 |
| P-017 | All Urdu-locale customer-facing strings (`_STRINGS['ur']`) | Native Urdu speaker review and sign-off before any string is sent to customers | CONSTRAINTS.md C-010 · backend/PENDING.md · FRONTEND-BACKEND-MAPPING.md §20 |
| MR-001 | Facebook/Instagram lead capture automation | Meta Business Manager setup by user | market-research-gap-register.md |
| MR-003 | Voice note transcription (Urdu/Roman Urdu/English) | Transcription provider selection + credentials | market-research-gap-register.md |
| MR-007 | Kuickpay payment adapter | Kuickpay API credentials | market-research-gap-register.md |

### Non-Negotiables Quick Reference

| Rule | Source | Consequence of violation |
|---|---|---|
| RTL must be wired at build time — not retrofitted | CONSTRAINTS.md C-001 | All 75 pages require structural rework |
| All API calls via `crm-api.js` with `DUMMY_MODE` flag | DESIGN-SPEC.md C-007 · CONTRIBUTING.md | Pages cannot switch to live backend without rework |
| `JAZZCASH_STUB_MODE=true` until P-016 resolved | CONSTRAINTS.md C-009 | Production payment failures; legal risk |
| All `_STRINGS['ur']` reviewed by native Urdu speaker before customer messages | CONSTRAINTS.md C-010 | Incorrect Urdu sent to customers |
| No country-specific logic in `core/` | architecture-overview.md | Breaks country isolation model; prevents future multi-country expansion |
| `core/*` must never import `adapters/pakistan/*` | architecture-overview.md | Enforced by ruff CI check; breaks on commit |
| All 96 existing library pages must stay at HTTP 200 after every push | REBUILD-PLAN.md | Non-negotiable regression gate |
| Every new doc must be added to DOC-CATALOGUE.md on the same day it is written | REBUILD-PLAN.md Sprint 0 rule | Doc invisible to future code audits |
| Never commit `.env` — use `.env.example` | .gitignore | Credential leak |
| All AI model outputs are advisory-only — no model result may trigger an autonomous mutation; every `CopilotSuggestion` requires `evidence_anchor` or is rejected at service layer (422) | `backend/docs/domain/ai-predictive-models.md`; `concurrency-control.md §F` | AI suggesting and auto-executing would violate the human-in-the-loop guarantee; evidence_anchor gap means suggestions are fabricated |
